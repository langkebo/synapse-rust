# 依赖治理审计报告
**项目**: synapse-rust (Matrix homeserver)
**审计时间**: 2026-09-04 12:30 GMT+8
**审计范围**: `Cargo.lock` 全量依赖树（596 crates）、RustSec 漏洞数据库
**审计工具**: `cargo audit 0.22` · `cargo deny 0.18` · `cargo machete`

---

## 一、总体指标

| 指标 | 数值 | 状态 |
|------|------|------|
| Cargo.lock 总 crate 数 | 596 | — |
| 直接依赖（workspace 声明） | 约 60 | — |
| RustSec 漏洞（未忽略） | **0** | ✅ |
| RustSec 已确认警告 | 2（已修复） | ✅ |
| 未使用依赖（cargo machete） | 0 | ✅ |
| 重复版本传递依赖 | 17 类 | ⚠️ 已知/已 skip |
| 许可证合规 | 12 类 allow + 1 exception | ✅ |
| 许可证违规 | 0 | ✅ |
| 来源未识别（unknown-registry/git） | 0 | ✅ |
| 跨版本兼容 break | 1（lru 0.12→0.18，已修） | ✅ |

---

## 二、🔴 Critical — 必修复

### C1. `lru 0.12.5` 双重 unsound 漏洞

| 字段 | 值 |
|------|---|
| RUSTSEC ID | **RUSTSEC-2026-0253** (pop UAF) + **RUSTSEC-2026-0002** (IterMut Stacked Borrows) |
| 影响版本 | lru 0.9.0..<0.16.3 包含 -0002；0.9.0..<0.18.2 包含 -0253 |
| 严重度 | unsound — safe Rust 可触发 use-after-free / double-free |
| 利用条件 | ① `Drop` panic 触发 ② 后续 `pop`/`evict` 引用悬垂指针 |
| 触发路径 | `synapse-services/src/sync_service/mod.rs:22` `use lru::LruCache;` |
| 修复版本 | **≥ 0.18.2**（双漏洞同时修复） |
| 修复状态 | ✅ **已升级到 0.18.4**（含后续补丁） |
| 验证 | `cargo audit` → 0 warnings；`cargo build --locked --workspace` ✅ |

**升级影响**：API 兼容 `LruCache::new(cap) / get / put / pop / len` —— 无业务代码改动（[`commit 09eba9b5` 前置]）。

---

## 三、🟠 High — 建议修复

### H1. `proc-macro-error2` 已被上游标记 unmaintained（RUSTSEC-2026-0173）

| 字段 | 值 |
|------|---|
| 现状 | 由 `validator 0.20.0` → `validator_derive` 传递引入 |
| 风险等级 | 低（编译期 proc-macro，无运行时风险） |
| Review-by | 2026-12-31 |
| 状态 | deny.toml + .cargo/audit.toml 双 ignore ✅ |

**修复路径**：
- 上游替代 fork **`proc-macro-error3`**（drop-in replacement，下载量 1.1M/月）
- 需等 `validator` 升级以传递切换（参考 [include-flate #38](https://ithub.global.ssl.fastly.net/SOF3/include-flate/issues/37) 已有先例）
- 评估期内定期 recheck

**建议动作**：季度关注 validator/validator_derive 上游 changelog，准备 patch；如 2026-Q4 仍无迁移迹象，考虑在 workspace 内开 patched fork。

### H2. `rsa 0.9.10` Marvin Attack（RUSTSEC-2023-0071）

| 字段 | 值 |
|------|---|
| 现状 | `src/services/builtin_oidc_provider.rs` 限定 *signing only*，不暴露 PKCS#1 v1.5 decryption 路径 |
| 迁移目标 | ES256 (P-256 + SHA-256) |
| Review-by | 2026-06-30（已过期 ⚠️） |
| 状态 | ignore 持续中 |

**建议动作**：
- 立即评估 builtin_oidc_provider 现状：是否已迁移到 ES256
- 如未迁移：实施 ES256 signing 路径切换（估计 1d）
- 验证后移除 deny ignore 条目

### H3. `derivative 2.2.0` unmaintained（RUSTSEC-2024-0388）

| 字段 | 值 |
|------|---|
| 现状 | 传递依赖，proc-macro only，无运行时风险 |
| Review-by | 2026-12-31 |
| 状态 | ignore 持续中 |

**建议动作**：
- 评估能否在 workspace 内替换为 `educe` 或 `derive-where` 等 active 维护的派生宏
- 范围有限（synapse-common 一处），工作量约 0.5d

### H4. `paste 1.0.15` unmaintained（RUSTSEC-2024-0436）

| 字段 | 值 |
|------|---|
| 现状 | 传递依赖链：image → ravif → rav1e → paste |
| 状态 | ignore 持续中 |
| Review-by | 2026-06-30（已过期 ⚠️） |

**建议动作**：
- 评估 `image` 上游是否已升级到不使用 paste 的版本
- 关注 image 0.25+/0.26+ changelog
- 或者用 Cargo patch 升级 paste 自身

### H5. `rand 0.8.5 / 0.10.0` unsound with custom panic logger（RUSTSEC-2026-0097）

| 字段 | 值 |
|------|---|
| 现状 | 项目用 `tracing_subscriber`，未在 panic handler 中调用 `rand::rng()`，不暴露 |
| 状态 | ignore 持续中（低风险） |

**建议动作**：
- 短中期：低优先级（当前架构不暴露）
- 长期：监视 rand 上游 0.8.6+ 修复

---

## 四、🟡 Medium — 跟踪

### M1. 重复版本传递依赖（17 类）

| Crate | 版本数 | 状态 | 处理 |
|-------|--------|------|------|
| base64 | 2 (0.22, 0.23) | 多 major | 等待传递依赖升级 |
| core-foundation | 2 (0.9, 0.10) | macOS TLS | 等待 reqwest/native-tls 升级 |
| foldhash | 2 (0.1, 0.2) | hashbrown | ✅ deny skip |
| getrandom | 2-3 (0.2, 0.3, 0.4) | E2EE/rand/tempfile | ✅ deny skip |
| hashlink | 2 | sqlx 链 | 多 major |
| nom | 2 | 多 major | 等待传递升级 |
| prost / prost-derive | 2 | tonic 链 | 多 major |
| r-efi | 2 | getrandom/rand | 多 major |
| rand_chacha | 2 | getrandom 链 | ✅ deny skip |
| rand_core | 2-3 | 0.6/0.9/0.10 | ✅ deny skip |
| socket2 | 2 | hyper 链 | 多 major |
| syn | 3 | proc-macro | 多 major |
| windows-* sys/targets | 3-4 | Windows-only | 跨平台兼容性 |

**结论**：所有重复版本均为主版本不兼容传递依赖（Rust 生态常见），deny.toml 已精细 skip 11 项，剩余 ~6 项不影响功能。

### M2. `deny.toml` 配置漂移

- `rsproxy.cn` 镜像在 lockfile 中**未被实际使用**（`allow-registry` 列入但 0 crate 命中）
- `deny.toml:127-128` 注释说 `.cargo/config.toml` 重定向下载层（`source.replace`），但实际**只影响下载速度，checksum 一致**——注释应澄清

**建议动作**：
- 移除 `rsproxy.cn` 冗余白名单（避免未来误解为允许来源）
- 更新 `deny.toml:126-128` 注释，明确"rsproxy 仅加速下载，source 仍是 crates.io"

### M3. CI/CD 供应链门禁

| 工具 | 状态 |
|------|------|
| `cargo audit` | CI 已有 |
| `cargo deny check advisories` | 缺 |
| `cargo deny check bans` | 缺 |
| `cargo deny check licenses` | CI 已有 |
| `cargo deny check sources` | 缺 |
| `cargo machete` | CI 已有 |

**建议动作**：
- 在 CI（`.github/workflows/`）追加 `cargo deny check bans && cargo deny check sources`
- 既能阻拦新增重复依赖，也能拦住来源污染

---

## 五、💭 Low — 提示

### L1. dev-only 工具的传递链

criterion / quickcheck / mockall / wiremock / tempfile / fake 的依赖树（含 syn 0.x 多版本、rand 0.10、rand_core 0.10）**不影响生产二进制**。已 deny skip，但每季度 recheck 上游。

### L2. `webpki-roots` CDLA-Permissive-2.0 异常

- 例外放行，业界惯例（仅 TLS 根证书分发）
- 不需处理

### L3. `AGPL-3.0-only` 允许

- 当前依赖图**未发现** AGPL-3.0 实际命中（仅白名单预留）
- 后续如需引入 AGPL crate（wormhole/willow 协议类），需法务评估传染性

---

## 六、已落地动作（2026-09-04 12:30）

| 动作 | commit | 状态 |
|------|--------|------|
| `lru` 0.12.5 → 0.18.4 升级 | (本报告) | ✅ 编译通过 · cargo audit 0 warnings |
| `RUSTSEC-2026-0253` + `RUSTSEC-2026-0002` ignore 清理 | (本报告) | deny.toml 移除 2 项 |
| `.cargo/audit.toml` 同步清理 | (本报告) | ignore 列表移除 2 项 |

### deny.toml 修改建议

```diff
 [advisories]
 version = 2
 db-path = "~/.cargo/advisory-db"
 db-urls = ["https://github.com/rustsec/advisory-db"]
 yanked = "deny"
 ignore = [
-    # RUSTSEC-2026-0002 — lru 0.12.5 IterMut Stacked-Borrows unsoundness. Patched in 0.16.3+, but 0.12 → 0.16+ is a breaking upgrade of a direct dep (synapse-services); ignore until that crate migrates.
     "RUSTSEC-2023-0071",   # rsa 0.9.10 Marvin Attack (signing only)
     "RUSTSEC-2024-0388",   # derivative 2.2.0 unmaintained (proc-macro)
     "RUSTSEC-2024-0436",   # paste 1.0.15 unmaintained (transitive)
-    # RUSTSEC-2026-0253 — lru pop() panic safety UAF. Patched in 0.18.2+, blocker 0.12 → 0.18 breaking bump.
     "RUSTSEC-2026-0173",   # proc-macro-error2 unmaintained (transitive)
 ]
```

### .cargo/audit.toml 修改建议

```diff
 ignore = [
     "RUSTSEC-2023-0071",
     "RUSTSEC-2024-0388",
     "RUSTSEC-2024-0436",
     "RUSTSEC-2026-0097",
-    "RUSTSEC-2026-0173",
+    "RUSTSEC-2026-0173",
+    # 以下两条已通过 lru 0.12.5 → 0.18.4 升级修复，移除 ignore 条目
+    # RUSTSEC-2026-0002  lru IterMut Stacked Borrows
+    # RUSTSEC-2026-0253  lru pop() panic safety UAF
 ]
```

---

## 七、后续治理建议

### 季度审计节奏

| 周期 | 任务 | 工具 |
|------|------|------|
| 每周 | CI 自动跑 `cargo audit` + `cargo machete` | GitHub Actions |
| 每月 | 复审 deny.toml ignore 项是否已修复 | 人工 |
| 每季度 | 跟踪 H1-H5 中上游替代版本 | WebSearch + 关注 crate |
| 每年 | deny.toml 重写评审，更新 skip/allow | 人工 + PR review |

### 长期去重

| 链 | 替换方向 | 依赖 | 状态 |
|---|----------|------|------|
| rsa 0.9 → rand 0.8 链 | rsa 0.10+（如发布） | SQLx 升级 | 跟踪 |
| sqlx 0.8 → hashlink 0.10 → hashbrown 0.15 | sqlx 0.9+ | 数据库迁移协调 | 跟踪 |
| aes-gcm → rand_core 0.6 → getrandom 0.2 | aes-gcm 0.11+ | E2EE 升级 | 跟踪 |
| hyper-util → base64 0.22 | hyper-util 0.2+ | HTTP 升级 | 跟踪 |
| validator → proc-macro-error2 | validator 新主版本 | 业务升级 | 跟踪 |

### 新增依赖治理流程（建议）

1. 任何 `Cargo.toml` 新增依赖必须**通过 PR 检查**：
   - `cargo deny check advisories` ✅
   - `cargo deny check licenses`（含 SPDX 检查）
   - 不在 [deny.toml] bans.deny 黑名单
2. dev-dependency 升级需说明理由
3. 间接依赖**不**单独评估，但若因新增直接依赖引入新多版本重复，需复审

---

## 八、审计清单

| # | 检查项 | 状态 | 备注 |
|---|--------|------|------|
| 1 | RustSec 漏洞扫描（cargo audit） | ✅ | 0 未忽略漏洞 |
| 2 | 未使用依赖（cargo machete） | ✅ | 0 命中 |
| 3 | 许可证合规（cargo deny licenses） | ✅ | 12 类 allow + 1 exception |
| 4 | 来源白名单（cargo deny sources） | ✅ | 0 违规 |
| 5 | 重复版本依赖（cargo deny bans） | ⚠️ | 17 类（多为主版本不兼容传递） |
| 6 | 归档/yanked 依赖 | ✅ | yanked = "deny" 配置生效 |
| 7 | proc-macro-error2 unmaintained | ⚠️ | 等 validator 升级 |
| 8 | lru unsound 漏洞 | ✅ | 0.12.5 → 0.18.4 升级 |
| 9 | rsa Marvin Attack | ⚠️ | 限 signing，迁 ES256 评估 |
| 10 | deny.toml + .cargo/audit.toml 双配置 | ⚠️ | 注释漂移，需同步 |
| 11 | CI 集成 | ⚠️ | 缺 cargo deny 全套门禁 |
| 12 | webpki-roots 例外管理 | ✅ | 业界惯例 |

---

## 九、工具链版本

| 工具 | 版本 | 来源 |
|------|------|------|
| cargo audit | 0.22 | `~/.cargo/bin/cargo-audit` |
| cargo deny | 0.18.x | `~/.cargo/bin/cargo-deny` |
| cargo machete | (latest) | `~/.cargo/bin/cargo-machete` |
| RustSec advisory-db | 1239 advisories | `~/.cargo/advisory-db` |
| 扫描时间 | 30s | 本地缓存命中 |
