# 依赖分析报告

> 日期: 2026-04-15
> 分析工具: cargo tree

## 重复依赖分析

### 1. base64 (0.21.7 / 0.22.1)

**来源**:
- `0.21.7`: config → ron → base64
- `0.22.1`: 项目直接依赖 + 多个上游 crate

**影响**: 低
**可操作性**: 无法直接解决，需要等待 `config` crate 更新其依赖

**建议**: 保持现状，这是上游依赖冲突

### 2. deadpool (0.10.0 / 0.12.3)

**来源**:
- `0.10.0`: deadpool-postgres v0.12.1
- `0.12.3`: deadpool-redis v0.18.0 + wiremock (dev)

**影响**: 低
**可操作性**: 可以尝试升级 deadpool-postgres

**建议**: 
```toml
# 检查是否有更新版本的 deadpool-postgres
deadpool-postgres = "0.14"  # 或最新版本
```

### 3. darling (0.20.11 / 0.23.0)

**来源**: 上游 proc-macro 依赖

**影响**: 极低（仅编译时）
**可操作性**: 无法直接解决

**建议**: 保持现状

### 4. core-foundation (0.9.4 / 0.10.1)

**来源**: macOS 系统库绑定，多个上游依赖

**影响**: 低
**可操作性**: 无法直接解决

**建议**: 保持现状

## 总结

### 可立即处理
- 无

### 可尝试优化
- **deadpool-postgres**: 检查是否有使用 deadpool 0.12+ 的新版本

### 无法处理（上游依赖）
- base64 版本冲突
- darling 版本冲突  
- core-foundation 版本冲突

## 建议行动

1. ✅ 接受大部分重复依赖为上游依赖冲突
2. ⏭️ 检查 deadpool-postgres 是否有更新版本
3. ⏭️ 定期运行 `cargo update` 保持依赖最新

## 依赖健康度评分

- **直接依赖**: ✅ 健康（无未使用依赖）
- **重复依赖**: ⚠️ 可接受（主要为上游冲突）
- **安全性**: ✅ 无已知漏洞（需定期 cargo audit）
- **维护性**: ✅ 良好

## 结论

当前的重复依赖主要由上游 crate 引入，不影响项目功能和性能。建议保持现状，定期更新依赖以获得上游修复。
