# 数据库审查报告（2026-09-17）

审查对象：`migrations/00000000_unified_schema_v12.sql`（唯一真相源，5426 行 / 230 表）
+ 实测数据库 `synapse_test`（已应用 v12，230 张 BASE TABLE）。
方法：机械解析 baseline + 对**真实 PG 实例**跑 `pg_catalog` 查询（不依赖正则推断）。

> 本报告中所有计数均来自 schema-qualified 的 `pg_catalog` 查询（`n.nspname='public'`），
> 避免 search_path 命中本机残留的旧测试模板 schema（本机存在 v11 指纹模板
> `test_isolation_template_cfb1fffffc06965a`，其 `events` 有 21 个索引——**不要**把它当成 v12 事实）。

## 结论摘要

| 维度 | 结论 |
|---|---|
| 主键 | ✅ 230/230 表都有 PK；7 张 integer PK 已升 BIGSERIAL（issue ⑦） |
| 列类型约定 | ✅ **无 `timestamp/timestamptz` 列**，全仓统一 BIGINT 毫秒（与代码约定一致） |
| 孤儿表 | ✅ 无「只有 PK、没有任何二级索引」的表 |
| 外键覆盖率 | ✅ 110 个 FK（129 处 REFERENCES 声明） |
| **冗余索引** | ✅ 28 组重复索引全部清除（§11.1） |
| **前缀冗余索引** | ✅ 8 处短索引已删除（§2） |
| **未索引的外键列** | ✅ 18 个 FK 列全部补齐前导索引（§12.2） |
| **FK 删除动作不一致** | ✅ `fk_events_room` 已改为 CASCADE（issue ⑤）；6 处重复外键已删除 |
| **可空外键** | ✅ `server_notices.user_id` / `widgets.room_id` 收紧为 NOT NULL（issue ⑥） |
| JSONB 索引覆盖 | ⚠️ 108 个 jsonb 列 vs 25 个 GIN 索引（83 列无索引） |
| 连接预算 | ✅ `max_connections=200`（docker/config/postgres.conf）+ CI 门禁（§12.4） |
| 分区 | ⚠️ 0 张分区表；已给出方案与判据，暂不实现（§12.5） |

## 1. 冗余索引（写放大，零读收益）—— 最高优先级

10 组「同一张表、同一列、同一唯一性、同一方法」的**完全重复**索引。每一组都让
每次 INSERT/UPDATE/DELETE 多维护一棵 B-tree，而查询计划只会用到其中一棵：

| 表 | 列 | 重复的两个索引 |
|---|---|---|
| `account_data` | (user_id, data_type) | `idx_account_data_user_type` / `uq_account_data_user_type` |
| `device_lists_stream` | (stream_id) | `device_lists_stream_pkey` / `idx_device_lists_stream_stream_id` |
| `event_to_state_groups` | (event_id) | `pk_event_to_state_groups` / `idx_event_to_state_groups_event_id` |
| `e2ee_secret_storage_keys` | (key_id) | `e2ee_secret_storage_keys_key_id_key` / `idx_e2ee_secret_storage_keys_key_id` |
| `feature_flag_targets` | (flag_key, subject_type, subject_id) | `uq_feature_flag_targets` / `idx_feature_flag_targets_lookup` |
| `federation_access_stats` | (server_name) | `federation_access_stats_server_name_key` / `idx_federation_access_stats_server` |
| `federation_blacklist` | (server_name) | `uq_federation_blacklist_name` / `idx_federation_blacklist_server` |
| `federation_cache` | (key) | `federation_cache_key_key` / `idx_federation_cache_key` |
| `megolm_session_keys` | (user_id, session_id) | `uq_megolm_session_keys` / `idx_megolm_session_keys_lookup` |
| `megolm_sessions` | (session_id) | `uq_megolm_sessions_session` / `idx_megolm_sessions_session` |

**成因**：baseline 由历史增量迁移折叠而成，早期「先建索引、后补 UNIQUE 约束」的写法被原样保留
（折叠脚本 `check_baseline_consolidation.py` 只校验「增量对象是否已吸收」，**不检测对象重复**）。
**修法**：删掉每组的 `idx_*`，保留约束索引（`uq_*`/`pk_*`）。DDL 层面零风险（约束索引必存在）。

## 2. 前缀冗余索引（8 处）

短索引的列是同一张表另一索引的严格前缀 ⇒ 长索引可完整服务于短索引的等值/范围查询，
短索引只剩「体积小」这一个（通常不足以抵消写放大）：

- `key_signatures.idx_key_signatures_target` ⊂ `uq_key_signatures_signature`
- `space_hierarchy.idx_space_hierarchy_space` ⊂ `uq_space_hierarchy`
- `push_devices.idx_push_devices_user` ⊂ `uq_push_devices_user_device_pushkey`
- `device_lists_outbound_pokes.idx_device_lists_outbound_pokes_user` ⊂ `pk_device_lists_outbound_pokes`
- `device_keys.idx_device_keys_fallback` ⊂ `uq_device_keys_user_device_key`
- `room_memberships.idx_room_memberships_user_membership` ⊂ `idx_room_memberships_user_status`
- `sliding_sync_tokens.idx_sliding_sync_tokens_user` ⊂ `idx_sliding_sync_tokens_unique`
- `sliding_sync_lists.idx_sliding_sync_lists_user_device` ⊂ `idx_sliding_sync_lists_unique`

判定需人工确认：若短索引是为「更小的索引体积换更热的查询」而保留，应写注释说明；
否则删除。**注意**：`*_unique` 里含 `COALESCE(conn_id,'')` 表达式，PG 仍可用其前导列做前缀扫描。

## 3. `events`（最热表）索引盘点：21 个

`public.events` 共 21 个索引（7 btree 全量 + 8 btree 部分 + 5 其他 + PK）。追加型最热表上
21 棵树意味着每次写事件都要维护 21 个索引——这是**写入路径的主要成本**。其中可确认的冗余：

- `idx_events_room_id (room_id)` —— 是 6 个以上索引的前缀 ⇒ 可删。
- `idx_events_room_stream_ordering (room_id, stream_ordering DESC)` —— 键与
  `idx_events_sync_covering (room_id, stream_ordering DESC) INCLUDE (...)` **完全相同**，
  后者是前者的覆盖版；两者并存属重复（保留覆盖版或保留小索引，二选一，需按同步查询的
  计划实测决定，别凭直觉）。

其余（`idx_events_not_redacted` / `idx_events_room_stream_ordering_not_redacted` /
`idx_events_friend_list` / `idx_events_friend_room` / `idx_events_room_soft_failed` /
`idx_events_state_dag_room`）都是**带 `WHERE` 的部分索引**，互不包含，属合理设计。

## 4. 外键列缺少以它为前导的索引（18 个）

父行删除/更新时，PG 必须扫子表找引用行；无索引 ⇒ 每次级联删除退化为全表扫。
`ON DELETE CASCADE` 在这张表上会因此变慢并长时间持锁：

```
blocked_users.blocked_id          friend_categories.user_id      friends.friend_id
moderation_actions.user_id        notification_delivery_log.notification_id / .user_id
presence_subscriptions.target_id  qr_login_transactions.user_id  room_summary_update_queue.room_id
scheduled_notifications.notification_id  secure_backup_session_keys.backup_id
server_notices.user_id            user_directory.room_id         user_notification_status.notification_id
widget_permissions.user_id        widget_sessions.user_id / .widget_id   widgets.user_id
```

其中 `notification_delivery_log` 有 **两个** 未索引 FK（`notification_id`、`user_id`），
而它按设计是**写入型流水表**，最需要索引。

## 5. 外键删除动作不一致

110 个 FK 中：**103 CASCADE、6 SET NULL、1 NO ACTION（默认）**。
唯一的例外：

```sql
-- events
fk_events_room FOREIGN KEY (room_id) REFERENCES rooms(room_id)   -- 无 ON DELETE
```

删除 room 时**必须**先删掉它全部 events（否则 FK 违约报错），与其他 103 个级联行为相反。
若这是刻意防误删，应显式写 `ON DELETE RESTRICT` 并加注释；否则应改为 CASCADE
（或按保留期清理流程处理），不应依赖默认值。

## 6. 可空外键（≥10 个）

`access_tokens.device_id`、`module_execution_logs.module_id`、`notification_delivery_log.user_id`、
`refresh_token_families.device_id`、`refresh_tokens.device_id`、`registration_token_usage.token_id`、
`server_notices.user_id`、`token_blacklist.user_id`、`widgets.room_id`、
`worker_task_assignments.assigned_worker_id`。

> ⚠️ **`widgets.room_id` 是本节的过期读数**：本清单来自 v11 时代的本地库，baseline 早已是
> `NOT NULL`，Rust 侧也已在 §13.4.1 裁定并改齐（`Option<String>` → `String`）。
> 以 §13.4.1 为准，不要按本节去"收紧"它。

NULL 语义 = 「无父行」，与 CASCADE 组合后行为是「父行删除时子行照留（因为 FK 为 NULL）」。
`server_notices.user_id` 这类**应为 NOT NULL** 的列建议收紧
（`server_notices` 是发给某用户的，`widgets` 属于某个房间——roomless widget 已裁定不合法，
见 §13.4.1，否则会出现「查不到归属」的脏数据）。

## 7. 类型与 DDL 卫生

- **PK 类型**：174 bigint / 45 text / **7 integer** / 3 uuid / 1 varchar(255)。
  7 张 `integer` PK 表（列名均为 `id`）：
  `device_lists_changes`、`room_ephemeral`、**`room_events`**、`room_tags`、
  **`search_index`**、`to_device_messages`、`user_filters`。
  其中 **`room_events`、`search_index`、`to_device_messages`、`device_lists_changes`、
  `room_ephemeral` 都是追加型高增长表**（`search_index` 是「每个被索引事件一行」）——
  int4 上限 21.4 亿，长期部署下这是**最现实的溢出/迁移风险点**，
  应在建库前就统一为 `bigint`（未发布项目，现在改代价最低）。
- **`SERIAL` 181 处、`IDENTITY` 0 处**：`SERIAL` 是遗留写法（隐式 sequence + DEFAULT + NOT NULL，
  且 sequence 所有权语义较弱）。功能无碍，但 `BIGSERIAL`/`SERIAL` 混用时容易出现
  上一条所述的类型意外。建议新表统一 `GENERATED BY DEFAULT AS IDENTITY`。
- ✅ **无 timestamp 列**（全 BIGINT 毫秒）——与代码约定一致，避免了时区/精度双重来源。
- **`VARCHAR(n)`**：31 个 255、2 个 50。`VARCHAR(n)` 的长度限制在 PG 里无性能收益，
  只在**业务确实要拒绝超长输入**时才有意义；若只是「随手写 255」，应改 `TEXT` 并让校验留在应用层
  （否则会出现「DB 报错、应用没校验」的 500）。
- **CHECK 约束 20 个 / 230 表**：覆盖率偏低，`status`、`*_type`、布尔语义列基本靠应用层自律。

## 8. JSONB 索引覆盖

108 个 jsonb 列，仅 25 个 GIN 索引。对**只整列读写**的 payload（如 `event_json`、`content`）无碍；
但凡做 `@>` / `?` / `->>` 过滤且无 GIN 的列都会全表扫。建议对已知热点
（`push_rules.actions`、`room_summaries` 的统计块、`federation_cache` 的响应体）逐列确认是否有
containment 查询；`idx_events_content_gin` 用 `jsonb_path_ops` 是正确选择（比默认 `jsonb_ops` 小且快）。

## 9. 运行参数与容量

- 本机 `max_connections = 100`，应用配置默认 `max_size = 20`（`synapse-common/src/config/database.rs`）。
  按 100 计算：**最多 5 个进程**（1 个主进程 + 4 个 worker/测试进程）就会打满；测试并行时
  会直接 starvation（仓库历史上有「1417 passed / 12 failed+7 timeout」的记录，根因即此）。
  建议：要么把 `max_connections` 提到 ≥200，要么在文档/启动检查里锁死
  `pool_size × 进程数 + 测试进程数 ≤ max_connections` 的不变式（当前无此门禁）。
- **0 张分区表**：`events`、`device_lists_stream`、`to_device_messages`、`presence`、
  `notification_delivery_log`、`audit_events` 都是**只增不减**型，长期部署必然需要
  保留期清理 + 分区。现有清理走 `RetentionService`/`prune_*` 脚本（应用层删除），
  删除大表历史数据会产生大量 dead tuple 与 autovacuum 压力，**分区（按月/按 stream 区间）+
  DROP PARTITION 才是结构性解法**。这是本报告中**唯一需要架构级投入**的项。

## 10. 迁移工程卫生（已改善，但仍有缺口）

- ✅ 单一 baseline（`migrations/00000000_unified_schema_v12.sql`）+ `docker/db_migrate.sh` 唯一入口；
  折叠校验脚本 `check_baseline_consolidation.py` 存在。
- ❌ **折叠脚本不检测「对象重复」**——正是第 1、2 节冗余索引能存活至今的原因。
  建议给 `check_baseline_consolidation.py` 加一条：按 `(表, 列集, 唯一性, 部分谓词)` 分组，
  组内 >1 即失败（可用 `CREATE (UNIQUE )?INDEX` 正则 + 归一化，无需真连库）。
- ⚠️ baseline 是「全量 DDL」，没有 down-migration；schema 回退只能靠重建（未发布项目，可接受）。
- ⚠️ `CREATE TABLE` 全部带 `IF NOT EXISTS`（1 处例外），可重复执行 ✅。

---

## 修复优先级（按「收益/风险」排序）

| # | 动作 | 风险 | 收益 |
|---|---|---|---|
| 1 | 删除第 1 节的 10 个重复索引（保留约束索引） | 极低（约束索引必存在） | 每张表少维护一棵 B-tree，纯写路径收益 |
| 2 | `events`：删 `idx_events_room_id`；`idx_events_room_stream_ordering` 与 `idx_events_sync_covering` 二选一（用 `EXPLAIN` 实测再定） | 低（需实测计划） | 最热表写放大直接下降 |
| 3 | 给 `fk_events_room` 显式声明删除策略（CASCADE 或 RESTRICT+注释） | 低 | 消除删除房间时的隐式行为差异 |
| 4 | 补第 4 节 18 个 FK 列的前导索引（先按实际查询确认，不是全补） | 低 | 级联删除不再全表扫 |
| 5 | `to_device_messages.id` 等 7 个 `integer` PK 评估是否改 bigint | 中（需重建表或 `ALTER TYPE`） | 消除 int4 溢出风险 |
| 6 | 收紧 `server_notices.user_id` / `widgets.room_id` 的 NOT NULL（若语义确实必填） | 中（需先清脏数据） | 消除归属不明的行 |
| 7 | 给折叠脚本加「重复对象」门禁（第 10 节） | 低 | 防止冗余索引再次进入 baseline |
| 8 | 连接预算不变式（门禁或启动检查） | 低 | 消除测试/多 worker 的 starvation |
| 9 | 追加型大表分区 + 按分区保留期 | 高（DDL + 应用改造） | 唯一的结构性容量解法 |

**未验证项**：本报告基于空库的元数据（本机 `synapse_test` 无真实数据），
因此「哪个索引其实没被用过」无法判断（需 `pg_stat_user_indexes` 的生产统计）；
第 2、4 项的取舍必须用真实 `EXPLAIN (ANALYZE, BUFFERS)` 复核后再动手。

---

# 11. 落地记录与两处更正（2026-09-17 执行）

## 11.1 更正一：重复索引是 **28 组**，不是 10 组
§1 的清单来自一条带 `limit 10` 的查询，**被截断了**。落地时在干净库上重新枚举，
实际为 **28 组**（另外 18 组：`federation_queue`、`oidc_auth_sessions`、
`registration_captcha`、`report_rate_limits`、`room_aliases`、`room_invite_allowlist`、
`room_invite_blocklist`、`room_memberships`×2、`room_summaries`、`room_summary_members`、
`space_summaries`、`state_group_state`、`threepid_validation_session`×3、
`upload_chunks`、`user_threepids`）。**§1 的"10 组"应以本节为准**，结论方向不变。
教训：报"共 N 条"之前必须确认查询没有 `limit`。

## 11.2 更正二：§5 的"不一致"其实是**同一列上的重复外键**，且它是真缺陷
§5 把 `fk_events_room` 的 `NO ACTION` 描述为"与其他 103 个 CASCADE 不一致，建议改成 CASCADE"。
**这个建议是错的，落地前的代码核查推翻了它**：

- `RoomStorage::delete_room`（`synapse-storage/src/room/mod.rs`，DB-04-b）**故意**先按 1000 行
  分批删除 events，最后才删 rooms 行 —— 因为 CASCADE 会在整张 `events` 表上取
  `AccessExclusiveLock`，阻塞该表所有并发读写。`NO ACTION` 在这里是**最终校验手段**。
- 真正的问题是**同一列上存在第二个外键**：表级 `fk_events_room`（NO ACTION）之外，
  baseline 尾部的 DO 块又加了 `fk_events_room_id ... ON DELETE CASCADE`
  （该 DO 块只检查自己的名字是否存在，因此不会与内联约束冲突而被跳过）。
- 后果两条：① CASCADE 仍然会触发，DB-04-b 想避免的表级锁照样发生；
  ② 代码末尾的 `DELETE FROM rooms` 会**静默成功**（残留 events 被级联删掉），
  "events 已删净"的校验形同虚设。
- 该缺陷**早已被记录**：`docs/audit/PROJECT_ACTUAL_ISSUES_2026-09-14.md:1370-1378`
  （"`fk_events_room_id` | **CASCADE** | 后面的 DO 块 … 两个都在"），只是从未修。

**同类缺陷共 6 处**（不止 events 一处）：`access_tokens.user_id`、`devices.user_id`、
`refresh_tokens.user_id`、`room_memberships.room_id`、`room_memberships.user_id`
各有 `fk_<table>_<col>` 与 `fk_<table>_<col>_id` 两个外键，两侧动作**都是 CASCADE**
（即删除冗余那个不影响行为）。全部 6 处已删除重复的那个，保留内联约束。

## 11.3 三项已落地（含验证）

| 优先级 | 动作 | 结果 |
|---|---|---|
| **1** | 删除 28 个与 PK/UNIQUE 完全重复的索引（**保留约束索引**） | baseline 从 720 → **692** 个索引关系 |
| **3** | `fk_events_room` **显式**写成 `ON DELETE NO ACTION` + 注释说明 DB-04-b 设计（**语义不变**）；并删除 6 个重复外键 | 外键 110 → **107**；`events` 现在只有 2 个外键（`fk_events_room` NO ACTION、`fk_events_redacted_by` SET NULL） |
| **6** | `scripts/check_baseline_consolidation.py` 新增 `duplicate_indexes()` 与 `duplicate_foreign_keys()` | 见 §11.4 |

**验证方式**：在全新数据库上应用修改后的 baseline。
```
apply exit = 0；tables = 230；indexes = 692；fks = 107
重复索引组 = 0；重复外键组 = 0
guard: ✅ 已吸收全部 0 个增量迁移的对象；无重复索引/外键
```

## 11.4 守卫的两个关键设计（否则会给出假阳性）
1. **外键重复只看"约束名不同"**：baseline 普遍存在「CREATE TABLE 内联声明 + 尾部幂等
   `IF NOT EXISTS (SELECT 1 FROM pg_constraint WHERE conname = ...)`」的双写，两者**同名**，
   是良性的（ALTER 是 no-op）。若按"同列同父"计数会误报 19 组。
2. **索引重复必须带谓词与唯一性**：`CREATE INDEX ... (a) WHERE x` 与 `CREATE INDEX ... (a)`
   不是重复；`UNIQUE` 与非 `UNIQUE` 也不是。键必须是
   `(表, 唯一性, 方法, 列集, WHERE 子句)`。

**RED 自证**：向 baseline 注入 `CONSTRAINT fk_events_room_probe ... ON DELETE CASCADE`
（与 `fk_events_room` 同列不同名）→ 守卫 `exit=1` 并点名 `fk_events_room, fk_events_room_probe`；
移除后 `exit=0`。

## 11.5 因此更新的仓库文件
`migrations/00000000_unified_schema_v12.sql`（删 28 索引 + 6 重复外键 + 显式 NO ACTION）、
`scripts/check_baseline_consolidation.py`（新守卫）、`migrations/INDEXES.md`（删 12 行失效行 + 说明）、
`migrations/README.md`、`tests/unit/migration_consistency_tests.rs`（其"折入索引"清单里
`idx_federation_queue_dest_created` 改成保留的 `idx_federation_queue_pending`）。

**仍未做**：优先级 2（`events` 上 `idx_events_room_id` 与两个同键索引的取舍，需真实
`EXPLAIN (ANALYZE, BUFFERS)`）、4（18 个 FK 索引）、5（int4 主键）、7（连接预算不变式）、
8（追加表分区）—— 见 §修复优先级表。

---

# 12. 优先级 2 / 4 / 5 / 7 / 8 的处理（2026-09-17 续）

## 12.1 优先级 2（events 索引取舍）—— 拆成"可安全做"与"必须先测量"两半
- **已做**：删除 `idx_events_room_id (room_id)`。`room_id` 是 ≥6 个其它索引的**前导列**，
  纯前缀冗余：`WHERE room_id = ?` 用任何一个复合索引的 btree 下降路径都同样高效。
- **未做（需实测）**：`idx_events_room_stream_ordering (room_id, stream_ordering DESC)`
  与 `idx_events_sync_covering (room_id, stream_ordering DESC) INCLUDE (event_id, sender,
  event_type, content, origin_server_ts)` **键完全相同**。二者各有理由：覆盖索引支持
  index-only scan（大表上省 heap 访问），但 `INCLUDE (content)` 让它**接近堆表大小**；
  非覆盖索引小得多，凡是要回表读内容的计划反而更省页。**取舍取决于真实数据分布与
  同步查询的实际计划，不能用元数据判断**（本轮已因"凭元数据下结论"栽过一次，见 §11.2）。
  测量方法（生产镜像上跑）：
  ```sql
  -- 1) 覆盖索引的真实体积与占比
  SELECT pg_size_pretty(pg_relation_size('idx_events_sync_covering')) AS covering,
         pg_size_pretty(pg_relation_size('idx_events_room_stream_ordering')) AS plain,
         pg_size_pretty(pg_relation_size('events')) AS heap;
  -- 2) 同步查询在两种索引下的计划与缓冲命中（分别 DROP 一个再跑，或用 pg_hint_plan 强制）
  EXPLAIN (ANALYZE, BUFFERS) SELECT event_id, sender, event_type, content, origin_server_ts
  FROM events WHERE room_id = $1 AND stream_ordering > $2 ORDER BY stream_ordering DESC LIMIT 50;
  ```
  判据：若典型同步查询能走 index-only scan 且 covering 体积可接受 → 删 non-covering；
  若多数计划仍需回表（或 covering 已接近堆表大小）→ 删 covering、保留小索引。

## 12.2 优先级 4（18 个缺索引外键列）—— 已完成
为 18 个外键列各补一个前导索引并折入 baseline；`events.redacted_by` 用**部分索引**
（`WHERE redacted_by IS NOT NULL`）——它是自引用外键（`→ events.event_id ON DELETE SET NULL`），
无索引时删除一条事件要全表扫 events 找引用者，而绝大多数行为 NULL，故部分索引足够且极小，
不必在 events 已有的 21 个索引上再加一棵全量 btree。

**验证**（全新库应用后）：`unindexed_fk_cols = 0`（原 18）；索引 692 → **709**。

## 12.3 优先级 5（7 个 int4 主键）—— 变更集已探明，**待编译恢复后执行**
已完成的依赖分析（可省一轮返工）：
- 这 7 张表（`device_lists_changes`、`room_ephemeral`、`room_events`、`room_tags`、
  `search_index`、`to_device_messages`、`user_filters`）**没有任何外键引用**它们的 `id`
  → 不存在需要同步改型的引用列（这是 int4→bigint 最容易漏的一步）。
- Rust 侧只有 **2 处**需要同步加宽：`synapse-storage/src/room_tag/mod.rs:10`
  与 `synapse-storage/src/search_index.rs:74` 的 `pub id: i32`；其余 5 张表在 Rust 里
  只被 `COUNT(*)` 之类聚合使用，从不解码 `id`。
**为何现在不做**：这是**跨层类型变更**（DDL + Rust 字段），而 sqlx 的动态 `query_as`
在类型不匹配时是**运行期**解码失败、编译期不报错；当前 workspace 因并发会话的 B3-5
中途态**无法编译**，也就无法用 `cargo check`/测试兜住这 2 处加宽。
盲改正好落在我本轮反复强调的"不可验证的改动"上。
**执行清单**（编译恢复后）：① baseline 中 7 张表 `id SERIAL→BIGSERIAL`；
② 上述 2 个字段 `i32→i64`；③ `cargo check --workspace --all-features` +
`cargo test --test unit --features test-utils`；④ 全新库应用一次 baseline 复核列类型。

## 12.4 优先级 7（连接预算不变式）—— 已完成，并纠正 §9 的一个错数
> ⚠️ **更正三**：§9 写的"应用默认 `max_size = 20`"是**错的** —— 20 是**已废弃**的
> `pool_size` 字段（源码注释明确它零引用），**生效上限是 `max_size`，默认
> `default_database_max_size() = 50`**。这让风险比我原先描述的更高：
> PostgreSQL 默认 `max_connections = 100` 时，**两个进程就打满**，实际只够 **1 个 worker**。

已落地：
- 新增 `scripts/ci/check_connection_budget.py`：从**源码**读出生效池上限（防脚本与代码漂移），
  再校验 `docker/deploy/README.md` 里**成文**的预算表，断言
  `池上限 × 进程数 + 保留 ≤ max_connections`；**预算项缺失时报错退出（exit 2）而不是静默通过**。
- `docker/deploy/README.md` 新增「连接预算（强制约束）」表：每进程 50 × 4 进程 + 保留 20
  = **220 ≤ 250** ✅，并写明"PG 默认 100 时只够 1 个 worker，4 worker 必须把
  `max_connections` 提到 ≥220（推荐 250）"。
- 已接入 `ci.yml` repo-sanity。
- **RED 自证**：把表里 `max_connections` 改成 100 → 门禁 `exit=1` 并打出
  「超预算 220 > 100」；改回 250 → `exit=0`。

## 12.5 优先级 8（追加型大表分区）—— **本轮不实现**，给方案与判据
不实现的三条理由，按重要性排序：
1. **会牵动主键**：PostgreSQL 的声明式分区要求**分区键必须包含在每个唯一索引/主键里**。
   `events` 的主键是 `event_id`（无分区键），要按 `origin_server_ts` 或 `room_id` 分区，
   就必须把 PK 改成 `(event_id, <分区键>)` 或 `(<分区键>, event_id)` —— 这会改变
   `ON CONFLICT`/去重语义与所有 `WHERE event_id = ?` 的计划，属**语义级**改动。
2. **应用侧不是透明的**：`synapse-storage` 里大量查询按 `room_id`/`stream_ordering`
   跨分区扫描；分区后需要保证分区裁剪条件出现在谓词里，否则计划退化为扫所有分区
   （比现在更慢）。这需要逐个热点查询审一遍。
3. **当前无数据、无生产**：分区的收益（DROP PARTITION 代替百万行 DELETE + vacuum）只在
   有量级数据时才成立；在空库上"先把结构布好"会把上述 1、2 的风险提前兑现而拿不到收益。

**建议的落地顺序**（真正需要时）：先只对**保留期最明确**的 `to_device_messages`
做试点（它按 `created_ts` 单调增长、查询几乎都带 `recipient` + 时间窗），
用「按月 RANGE 分区 + 保留 N 个月 → `DROP PARTITION`」跑通清理路径，
再评估 `events` / `device_lists_stream` / `audit_events`。
**在此之前**，应先确认现有清理路径是否真的在跑（`RetentionService` 的
`run_data_lifecycle_cycle`、`prune_*` 脚本）——若 pruning 未启用，分区只是把同样的问题
推后，而不是解决它。

## 12.6 本节落地后的量化
| 指标 | 本轮前 | 本轮后 |
|---|---|---|
| 索引关系（全新库） | 720 | **709**（−28 重复 +18 外键索引 −1 前缀冗余） |
| 无前导索引的外键列 | 18 | **0** |
| 重复索引组 / 重复外键组 | 28 / 6 | **0 / 0** |
| 连接预算 | 无约束、无文档（且 §9 数字有误） | **成文 + CI 门禁 + RED 自证** |

---

# 13. 优先级 5 的执行结果：**原判断被推翻，真正的缺陷在 Rust 侧**

> ⚠️ **更正四（最重要的一次）**：§7 与 §12.3 声称"7 张表用 `integer` 主键，有 int4 溢出风险，
> 应把 `SERIAL → BIGSERIAL`"。**这个结论是错的** —— 它是从**过期的本地库**读出来的，
> 而我当时没有像对其它优先级那样在**全新库**上复核。

## 13.1 事实（全新库应用当前 baseline）
- 7 张表的 `id` **本来就是 `bigint`**：`device_lists_changes`、`room_ephemeral`、`room_events`、
  `room_tags`、`search_index`、`to_device_messages`、`user_filters` 全部为 `id BIGSERIAL PRIMARY KEY`。
- 全新库 PK 类型普查：**bigint 181 / text 45 / uuid 3 / varchar(255) 1 —— `integer` = 0**。
- 也就是说 **DDL 侧无需任何改动**（第 1 步是 no-op）。

## 13.2 那为什么本地库显示 integer？
`synapse_test` 是从**旧 baseline**（v11 或修好之前的 v12）建的，`room_tags.id` 等列仍是 `int4`；
本地模板 schema 的指纹也还是 v11 的 `cfb1ffff...`。**本地库是过期的，不是权威的** ——
权威只有一个：**对当前 baseline 做全新应用**。这正是我在优先级 1/4/6/7 上用对了、
却在优先级 5 上漏掉的一步。

## 13.3 反过来暴露的真实缺陷（已修）
真实风险方向是**相反的**：baseline 是 `bigint`，而 Rust 侧有两个字段是 `i32`。
sqlx 不做 INT8→i32 的隐式转换，因此在**全新部署**上会**运行期解码失败**；
而过期的本地库（int4）恰好让测试全绿 —— **本地绿、上生产挂**。

已修（宽到 i64，与 baseline 对齐）：
- `synapse-storage/src/room_tag/mod.rs` 的 `RoomTag.id`（`SELECT id, ... FROM room_tags` 直接解码该列）
- `synapse-storage/src/search_index.rs` 的 `SearchIndexCursor.id`（游标 `created_ts|id`，
  id 超过 2^31 会在 parse 时溢出）
- 连带：`test_mocks/room_tag.rs` 的 `next_id`、`search_index.rs` 测试里的 `i32::MAX` 夹具

**验证**：`cargo check --workspace --all-targets --all-features` = **0 error**；
`cargo test --test unit --features test-utils` = **1815 passed / 0 failed**；
`cargo test -p synapse-storage --lib` 中 `room_tag::db_tests` 在**重新按 baseline 建库后由红转绿**
—— 这条正是"Rust 与 baseline 类型对齐"的直接证据（用过期库时反而红，因为 int4 解不进 i64）。

## 13.4 顺带发现的两个既有问题（**均已裁定并修好**）

### 13.4.1 `widgets.room_id` —— 裁定：**房间外的"全局 widget"在本实现中不合法**

原状：baseline 里是 **NOT NULL**，但 `widget::db_tests` 插入 `NULL`
（`null value in column "room_id" ... violates not-null constraint`），
且 Rust 的 `Widget.room_id` 是 `Option<String>`。三者不一致。

**裁定：`room_id` 必填，房间作用域是唯一合法形态。** 证据：

1. **DB 是唯一真相源且已经裁定 NOT NULL**（`00000000_unified_schema_v12.sql:3062`，
   v12.0.1 / issue ⑥ 明确收紧）。Rust 侧 `Option` 只是没跟上。
2. **没有 account-widget 的客户端可发现面**：唯一列表路由是
   `GET /_matrix/client/v1/rooms/{room_id}/widgets`（房间作用域）。
   `WidgetStoreApi::get_user_widgets` / `WidgetService::get_user_widgets` 在生产代码里
   **零调用者**（只有 trait 转发 + 测试）。因此 roomless widget 是"写了读不回来"的记录。
3. **授权锚点只有房间成员关系**：`get_widget_with_access` 对 roomless widget 只能退化成
   "owner 或显式授权"，没有成员关系门。
4. **Matrix 语义澄清**：Matrix Widget API 确实存在 account-level widget
   （`m.widgets` account data），但本服务**没有实现该面**（无 account data 读写、无列表路由）。
   所以 `Option<String>` 不等于"支持全局 widget"，只是存储层一个不可达分支，
   属于铁律 #1 禁止的"先留着以后可能有人用"的中间态。
5. **正确性收益**：`Option` + NOT NULL 的组合让缺 `room_id` 的创建请求返回
   **500（23502）** 而不是 400 —— 类型系统允许 DB 无法表示的状态。改成 `String` 后这是
   **编译期**保证，与 13.3 的 i32/bigint 同源（本地绿、上线挂）。

已修：`Widget.room_id` / `CreateWidgetParams.room_id` / `CreateWidgetRequest.room_id` /
`WidgetApiResponse.room_id` 全部 `Option<String>` → `String`；
`create_widget` 缺 `room_id`/空串 → **400 `M_BAD_JSON`**；
`get_widget_with_access` 去掉 roomless 分支（成员关系成为唯一锚点）；
顺带删除**死代码** `CreateWidgetBody`（handler 走 raw JSON 手工校验，该类型只剩自己的测试）；
`widget::db_tests` 的 9 处 `room_id: None` 夹具改为建真实房间（新增 `ensure_widget_room`）。
新增集成测试 `test_create_widget_rejects_missing_room_id`（缺字段 + 空串两种 body）。
`get_user_widgets` 仍是"已实现但未接线"的服务能力，另案处理（本轮不动语义）。

（§6 把 `widgets.room_id` 列为"可空外键"是**过期库**的读数，以本节为准。）

### 13.4.2 fmt 门禁红 —— 裁定：修生成器，不在棘轮里豁免生成文件

原状：`synapse-web/src/routes/derived_route_table_{always,oidc,worker}.inc.rs`
（302 KB，**生成文件**）未格式化，fmt 棘轮 `current=999 baseline=0`。它是被
**B4-5d 给 fmt 棘轮扩了扫描面**（新增 `synapse-web`）才暴露的 —— 之前棘轮不扫这个 crate。

**裁定：在棘轮里排除生成文件是错的**（那是"扫描面盲区"，AGENTS.md 铁律 #8 的推论：
把代码移出门禁扫描面 = 静默逃逸）。根因在生成器，共三处：

1. `emit_data_per_profile()` 的输出**直接写盘，从未过 rustfmt**（只有旧的单体 `emit()` 过了）；
2. `_rustfmt()` 在 rustfmt 不可用/失败时**静默返回未格式化文本**（`return text`）；
3. `main()` 的 `--check` **只声明未实现** —— 而 `scripts/contract/check_route_contract.sh:48`
   正是用 `--check` 调它，所以那个"漂移门禁"实际上**每次都重写工作树并恒返回 0**。

已修：`.inc.rs` 三份 + `derived_routes.rs` 全部过 `_rustfmt`；`_rustfmt` 失败改为
`raise`（不再静默回退）；`--check` 实现为纯比较（不写盘），漂移时列出文件并 exit 1；
顺带删除生成器里两处死代码（`emit`/`emit_raw` 单体渲染器、被同名后定义遮蔽的
`_generate_test_module`）—— 删除前后 4 份输出**逐字节相同**。

验证：三份 `.inc.rs` 的 diff 只含空白与行尾冗余逗号（token 流去空白/逗号后 MD5 相同、
字符串字面量序列相同）；`derived_routes.rs` 重生成后**零 diff**；
`--check` 由红（列出 4 个文件）转绿；fmt 棘轮 `current=0 baseline=0`；
两者均用故意违规探针自证会变红。

## 13.5 修 13.4.2 时连带发现的 **P0：`create_router` 在启动时 panic**（已修）

**症状**：`synapse-web/src/routes/assembly.rs:331`（修前）注册了
`"/auth/:auth_type/fallback/web"`。工作区是 **axum 0.8.9**，其 `Router::route` 走
`panic_on_err!(path_router.route(...))`，而 `PathRouter::route` 先执行
`validate_v07_paths`，于是 `:param` **不是降级而是 panic**：

```
thread '...' panicked at synapse-web/src/routes/assembly.rs:330:10:
Path segments must not start with `:`. For capture groups, use `{capture}`.
```

`create_router` 是唯一装配点，所以这等于**服务端根本无法启动**。`cargo check` 看不见
（只是一个 `&str`），只建 `AppState` 的单测也不经过它。

**为什么长期没人发现（双重掩盖）**：

1. `synapse-web/src/routes/derived_route_table_always.inc.rs` 里那条路径也带着（生成自源码），
   3cefbfcf「修 path_params 格式不匹配」时把 fixture 的 `path_params` 记成了 `[]` ——
   即**把一个带参数的路径写成了无参数**，缺陷被固化进 SDK 契约。
2. 集成测试当时是**假绿**（见 §13.6）：schema 克隆失败被 `.ok()?` 吞掉，每个测试早退，
   于是没有任何测试真正执行过 `create_router`。

**已修**：路径改为 `/auth/{auth_type}/fallback/web`（handler 本来就取
`Path(auth_type): Path<String>`）；6 份 ledger fixture 的路径与 `path_params` 同步为
`["auth_type"]`；派生表重新生成；`ROUTE_CONTRACT.md` 重新生成。

**新增两道守卫（均自证会红）**：

- `scripts/ci/check_axum_path_syntax.py`（**新**，接入 `check_route_contract.sh`）：
  扫描 `synapse-web/src` 全部 `.route()/.route_service()/.nest()` 字面量，
  拒绝任何 `:` / `*` 开头的路径段。实测 971 条路径字面量中恰好命中这一条；
  红探针（探针文件 + 重新引入 `:auth_type`）EXIT=1 且点名两处。
- 集成回归测试 `api_auth_routes_tests::test_auth_fallback_web_route_matches_a_real_auth_type`：
  建 router + 用 `m.login.password` 真实请求断言 200 且页面回显该 stage。
  把 `{auth_type}` 改回 `:auth_type` → 该测试立刻以同一条 panic 变红。
  此前**没有任何测试碰过这个路由**，这是它能长期存活的直接原因。

**验证**（`synapse` 角色临时 `SUPERUSER` 以绕过 §13.6 的克隆缺陷，跑完已还原为 `NOSUPERUSER`）：
`api_widget_tests` **19/19 通过**、`api_route_ledger_tests` **14/14 通过**
（顺带把两份 `route_ledger_*.snapshot` 里缺失的这条路由补上：1127→1128、1138→1139）。

## 13.6 两个**未修**的既有缺陷（需裁定 / 另案）

### 13.6.1 集成测试**静默假绿**：schema 克隆失败被吞（严重）

**机制**：`tests/integration/mod.rs:462` 用
`synapse_test_utils::acquire_pooled_schema().await.ok()?` 取隔离 schema；失败即
`TestContext::new()` 返回 `None`，而调用方一律是
`let Some(app) = setup_test_app().await else { return; };` —— **测试直接"通过"**。
错误被完全吞掉（`RUST_LOG=debug` 全量日志里一条 ERROR/WARN 都没有）。

**实测根因**（把 `.ok()?` 换成打印错误的探针后）：

```
clone of test_63297_1_... from test_template_v2_a2fc82a87d9e59c7 failed:
error returned from database: permission denied for language c
```

模板 schema 里装着 `pgcrypto`，克隆逻辑用 `pg_get_functiondef` **逐条重放函数**，
其中 `digest/hmac/crypt/...` 是 `LANGUAGE c` 的扩展函数；`synapse` 角色
`rolsuper = f`（本机 Homebrew PG，`ljf` 才是超级用户），于是重放被拒。
超级用户下克隆成功、测试才真正执行 —— 这正是本轮 19/19 与"假绿 19/19"的区别。

**修法建议（两条一起做）**：

1. 克隆时**不要重放扩展所属函数**，改为按模板的 `pg_extension` 在克隆 schema 内
   `CREATE EXTENSION IF NOT EXISTS <name> SCHEMA <clone>`（pgcrypto 是 trusted，
   库 owner 即可创建）。判据：`NOT EXISTS (SELECT 1 FROM pg_depend d WHERE d.objid = p.oid AND d.deptype = 'e')`。
2. 把 `.ok()?` 的静默吞错改成**显式失败或至少一次性大声告警** ——
   一个"基础设施挂了就全绿"的测试套件比没有测试更危险（与本仓库
   `P5_test_fixture_error_swallowing` 是同一类问题）。

**临时绕过（仅本机，非修复）**：`psql -d postgres -c "ALTER ROLE synapse SUPERUSER"`。
本轮用它取得真实证据后**已还原**。

### 13.6.2 `/capabilities` 的 `m.room_versions.available` 丢掉了 v12/v13（需裁定）

`api_route_snapshots_tests::snapshot_capabilities_v3` 在**真实**运行时是红的（此前被
§13.6.1 掩盖）。差异有两部分：

1. 响应里不再有非标准的 `capabilities.rooms.room_versions` 块 ——
   这是 **0cb00bb4 的刻意修复**（`versions.rs:112-114` 有明确注释：spec 没有该字段，
   与 `m.room_versions` 重复，客户端也不读），**新快照是对的**。
2. `m.room_versions.available` 只剩 `1..11`，**v12/v13 整个消失**。
   而 `synapse-common/src/room_versions.rs:112-113` 明确写着 v12/v13 是
   `stable_parse_only`，意图是"**降级为 parse+join+federate-only 可用**"。
   根因是 `client_room_versions_capability()`（`room_versions.rs:154-161`）
   按 `capability.can_create` 过滤，而 federation 版本用的是 `can_federate` ——
   两个映射的判据不一致。

**未修的理由**：这是协议能力声明语义（客户端 `available` 到底该列"能创建"还是
"能加入/解析"），AGENTS.md 要求 room-version 能力必须与实际行为一致且保守；
正确取值需要一次显式裁定。**因此没有 `cargo insta accept`** —— 直接接受会把这个
（很可能错误的）行为固化，而提交的旧快照又带着已经作废的 `rooms` 块。
`.snap.new` 已删除，避免留下误导性残留。



