enabled: true
default:
  per_second: 50
  burst_size: 100
endpoints:
  - path: "/_matrix/client/versions"
    per_second: 10
    burst_size: 30
  - path: "/.well-known/matrix/client"
    per_second: 10
    burst_size: 30
  - path: "/.well-known/matrix/server"
    per_second: 10
    burst_size: 30
  - path: "/health"
    per_second: 10
    burst_size: 30
ip_header_priority:
  - "x-forwarded-for"
  - "x-real-ip"
  - "forwarded"
include_headers: true
exempt_paths:
  - "/"
exempt_path_prefixes: []
endpoint_aliases: {}
fail_open_on_error: true
sync:
  # Sliding sync performs long-polling and bursty catch-up after login. Disabling
  # the dedicated sync limiter avoids false-positive 429s during local use of
  # https://matrix.test while keeping the general limiter enabled elsewhere.
  enabled: false
  initial:
    per_second: 5
    burst_size: 10
  incremental:
    per_second: 50
    burst_size: 100
