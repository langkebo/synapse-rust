# Extension Map Configuration
# Updated: 2026-05-07 (consolidated from 5 individual extension files)
#
# Format: <filename>=<feature_name>
# Each line maps a migration file to a feature flag used in ENABLED_EXTENSIONS.
#
# All feature-gated tables use IF NOT EXISTS. Applying the merged extension
# file unconditionally is safe; the map preserves per-feature filtering for
# deployments that selectively enable extensions.

00000001_extensions.sql=extensions-core
