MODULE=auth_compat	SEVERITY=High	TC=AUTHC-014	DESC=GET /login advertises m.login.token flow but POST /login handler requires username/password (returns 400 Username required for m.login.token request). m.login.token SSO/QR login flow cannot complete.
MODULE=sync	SEVERITY=Medium	TC=SYNC-007	DESC=GET /sync with invalid  token returns 200 (full sync). Matrix spec mandates 400 M_INVALID_PARAM for malformed since. Silent fallback risks clients missing state changes (treated as 'no new events' when since is unknown).
MODULE=sync	SEVERITY=Low	TC=SYNC-008	DESC=GET /_matrix/client/v3/events is deprecated in Matrix spec but the implementation still serves events with HTTP 200. Should return 410 Gone (or at minimum 404) to discourage client use.
MODULE=room	SEVERITY=Medium	TC=ROOM-032	DESC=DELETE /_matrix/client/v3/rooms/{room_id} returns 405 — endpoint is not implemented. PRD requires admin room-deletion capability; should be exposed under /_synapse/admin/v1/rooms/{room_id} or as Matrix-spec DELETE.
