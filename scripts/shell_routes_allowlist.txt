# Empty Success Response Allowlist
# This file contains reviewed empty-success matches from the scanner.
# Entries may be either:
# - tracked placeholder debt that still needs cleanup
# - spec-allowed / real ACK endpoints that intentionally return an empty success body
# Format: filename:line_number
# New shell routes not in this list will cause CI to fail

# AppService routes (spec-allowed / real ACK after successful namespace or transaction handling)
app_service.rs:428
app_service.rs:446
app_service.rs:464

# E2EE routes (real ACK after persisted send-to-device, cross-signing, or key-request cancellation)
# Migrated from monolithic e2ee_routes.rs into e2ee/ sub-modules (commit b0ef29db)
e2ee/backup.rs:186
e2ee/devices.rs:360

# Sticky event routes (storage mutation ACK)
sticky_event.rs:131
sticky_event.rs:149

# Room tag routes (Matrix-style tag mutation ACK)
tags.rs:119
tags.rs:133

# VoIP routes (call state mutation ACK)
voip.rs:234
voip.rs:292
