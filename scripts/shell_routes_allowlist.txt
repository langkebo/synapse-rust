# Empty Success Response Allowlist
# This file contains reviewed empty-success matches from the scanner.
# Entries may be either:
# - tracked placeholder debt that still needs cleanup
# - spec-allowed / real ACK endpoints that intentionally return an empty success body
# Format: filename:line_number  (relative to synapse-web/src/routes/)
#
# BOTH directions are enforced by tests/unit/placeholder_scan_tests.rs:
# - a new `Ok(empty_json())` site not listed here fails CI;
# - an entry here that matches no site fails CI too. This file must describe
#   reality, not accumulate exemptions — on 2026-09-19 it held 37 entries of
#   which 26 matched nothing (the routes had moved crates and been reflowed),
#   which nothing detected because the scanner was itself pointed at the old
#   `src/web/routes` path.
#
# Consequence: any `cargo fmt` or edit that shifts one of these lines makes the
# test fail with both the new unlisted hit and the now-stale entry. Update the
# number in the same commit — that is the intended signal, not noise.

# AppService routes (spec-allowed / real ACK after successful namespace or
# transaction handling)
app_service.rs:518
app_service.rs:537
app_service.rs:556

# Sticky event routes (storage mutation ACK)
sticky_event.rs:134
sticky_event.rs:152

# Room tag routes (state mutation ACK)
tags.rs:105
tags.rs:119

# VoIP routes (signalling ACK)
voip.rs:243
voip.rs:301

# E2EE routes (real ACK after persisted send-to-device, cross-signing, or
# key-request cancellation)
e2ee/backup.rs:179
e2ee/devices.rs:373
