# D-36 守卫 B 的豁免名单（生产 INSERT 未覆盖的必需列）
#
# 键格式：`<repo-relative-path>::<table>`，**不含行号**（理由同
# `scripts/ci/test_ddl_allowlist`）。
#
# 守卫规则（在**迁移到最新 baseline 的 schema** 上查 catalog，不读业务代码）：
#   R1  `is_nullable='NO' AND column_default IS NULL AND is_identity='NO'
#        AND is_generated='NEVER'` 的列必须出现在 INSERT 的**字面量列清单**里；
#   R2  baseline 生成的 `ck_<table>_user_id_format` 家族（见
#        `migrations/00000000_unified_schema_v12.sql` 的 DO 循环）里 `user_id`
#        为 `NOT NULL` 的表，列清单必须含 `user_id`。R2 专抓
#        `NOT NULL DEFAULT ''` 这种"有默认值但默认值违反 CHECK"的形态（D-10）。
#
# 列清单为运行期拼装（`format!` / `QueryBuilder`）的 INSERT 记为「未覆盖」并在此登记，
# 不判通过（那些站点由 §7 D-14 跟踪）。

# D-30：`presence_subscriptions` 的 `is_undefined_column_error` 回退分支（不可达）。
# 该分支写 `(user_id, friend_id, created_ts)`，两个真列名其实是
# `subscriber_id`/`target_id`，且它只在主分支报 42703 时才走 —— 主分支正常，
# 所以这段代码既不可达又必然失败。按 §8.2 W4 删除 4 个回退分支后，必须删除本行。
synapse-storage/src/presence/mod.rs::presence_subscriptions
