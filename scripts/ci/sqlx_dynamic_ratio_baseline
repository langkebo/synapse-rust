# 2026-09-22: dynamic 由 2146 上调到 2147（+1）。
#   (+1) `synapse-storage/src/refresh_token/mod.rs` 的
#        `test_db_get_rotations_breaks_ties_on_the_same_millisecond_by_id` 夹具：
#        它必须**显式写同一个 `rotated_ts`** 来构造"同毫秒并列"，而公开 API
#        （`record_rotation`）总是自己打 `current_timestamp_millis()`，无法注入。
#        为什么不用 `query!` 宏：`#[cfg(test)]` 里的查询宏不会被
#        `cargo sqlx prepare --workspace`（默认 target 集）收集，而
#        `-- --all-targets` 又会因为测试目标缺少 feature 而 E0432 ⇒
#        `SQLX_OFFLINE=true … --all-targets` 会报 `no cached data`（均实测）。
#        因此这类**测试夹具**只能走动态 SQL；收紧方向：等 sqlx 支持为 cfg(test)
#        生成元数据，或把该夹具搬到独立测试目标（`tests/`）后重估。
#
# 2026-09-22: dynamic 由 1501 上调到 1504（+3），并修正计数器缺陷后更新为 2146。
# 修正事项：
#   1. 正则未匹配 turbofish 形式 `sqlx::query_as::<T>(` / `sqlx::query_scalar::<T>(`
#   2. 注释行 (`//! sqlx::query(..)`) 误算为调用
# 两者导致：实际动态调用数 = 计数值 + 643 (turbofish) - 1 (注释误算)
# （方法：`git grep -cE 'sqlx::query(_as|_scalar)?\(' <rev> -- <扫描目录>` 逐文件求和）。
#
#   (+1) `a283328f`（指标埋点接线）新增的 `synapse-common/src/db_query_metrics.rs`
#        —— 但这一处**不是真的动态查询**：它是该文件 doc 注释里的一句
#        `//! this crate's storage layer has ~1700 direct sqlx::query(..) call sites`。
#        计数正则不看注释，因此把散文里的 "sqlx::query(" 也算成了调用。
#        证据：`git grep -c` 在 `6c6216cc` 与 `a283328f`/HEAD 之间**只有这一个文件**
#        有差异（1501 → 1502），且该文件里唯一的命中就在 `//!` 行上。
#        ⇒ 按现口径，写文档提到这个 API 就会抬高棘轮。修法见下方"已知计数缺陷"：
#        应先让计数器剥掉注释/字符串，再一次性重测基线（届时本 +1 自然消失）。
#
#   (+2) 本轮的 `synapse-test-utils/src/lib.rs`：`schema_pool_idle_reclaimer_tests::
#        expired_parked_schema_is_dropped_from_the_database` 用两次
#        `SELECT to_regnamespace($1)::text` 做**双向**断言（清扫前必须存在、清扫后必须
#        不存在）。只断言"不存在"会让查询写错时空转通过——这正是本仓反复踩到的
#        "门禁不会变红"形态，因此前置的存在性断言是刻意的，不能为省 1 处计数而删。
#        这两处无法静态化：`to_regnamespace` 是 test-only 夹具的探针，且本文件的
#        测试模块必须内联在 crate 里（要访问私有的 `SCHEMA_POOL` / `take_expired`），
#        而 `tests/` 独立目录不在本棘轮的扫描范围内。
#        收紧方向：若将来 `SCHEMA_POOL` 的私有访问面被一个 `#[cfg(test)] pub` helper
#        暴露出来，这个用例可以搬到 `tests/`（不计数），届时回收 2 处。
#
# 已知计数缺陷（本轮新增登记，未在本轮修）：① 正则**不看注释**（上面那 +1 即此类）；
# ② 不匹配 turbofish 写法 `sqlx::query_as::<_, T>(...)`（文件末尾早有登记）。
# 两者方向相反、都无法靠调基线长期抵消；正确修法是先剥注释/字符串、补 turbofish 分支，
# 再一次性重测并重设基线（含历史各条记录的口径说明），作为独立条目处理。
# 2026-09-21: dynamic 由 1499 上调到 1501（+2），来源是 `6c6216cc`（MSC4155 账号级邀请
# 策略）对 `synapse-storage/src/invite_blocklist.rs` 的改动。逐行核对（把两个 commit 的
# 该文件分别导出后按 `#[cfg(test)]` 边界统计）：
#   改动前: production=6  test=4  （合计 10）
#   改动后: production=4  test=8  （合计 12）
# 即**生产侧动态 SQL 减少 2 处**（两处 `SELECT 1 … LIMIT 1` 探测被合并进一个
# `query_as::<_, (bool,bool,bool)>`，两处 DELETE 改进事务），净增的 2 处全部是
# `db_tests` 里的 poison-row 触发器夹具（`CREATE TRIGGER` / `set_config(
# 'synapse.test_poison_user', …)` / `DROP TRIGGER`）—— DDL 与 `set_config` 无法用
# `query!` 静态化，属本文件既有"测试基础设施"类别。
# 触发原因：`6c6216cc` 未同步上调基线，main 的 Repo Sanity 因此红（run 35548900072）。
# 2026-09-20: dynamic 由 1484 上调到 1499，全部来自测试基础设施（schema 连接级租约在
# test_schema_guard.rs / 各 test_utils.rs 的 after_connect 里新增的 pg_advisory_lock_shared /
# pg_try_advisory_lock / pg_advisory_unlock 等带绑定的动态语句）；生产路径未新增动态 SQL。
# SQLx 动态/静态查询棘轮基线
#
# 由 scripts/ci/check_sqlx_dynamic_ratio.sh 读取。
#
# 语义（与 scripts/check_fmt_ratchet.sh 同型）：
#   * dynamic  **不得增加** —— 新增动态 SQL 即 CI 失败
#   * static   **不得减少** —— 编译期校验的查询不得被改回动态
#
# 因此这是一个"不会变坏"的门禁，而不是"必须先把全部调用改完"的门禁。
# 迁移取得进展后，请**同时**下调 BASELINE_DYNAMIC、上调 BASELINE_STATIC，
# 让棘轮单向收紧。
#
# ── 计数口径 ─────────────────────────────────────────────────────────────────
# 扫描范围：src/ + 全部 workspace crate 的 src/
#   （synapse-common, synapse-cache, synapse-storage, synapse-e2ee,
#     synapse-federation, synapse-services）
# 排除：target/、.git/、.claude/（.claude/worktrees 下有旧仓库副本，
#       会随本地 worktree 状态漂移，导致基线无法稳定复现）
#
# ⚠️ 计数**包含** crate 内 `#[cfg(test)]` 内联测试模块
#    （例如 synapse-storage/src/event/db_tests.rs）。
#    独立测试目录 tests/ 与 artifacts/ **不在**扫描范围内。
#
# ⚠️ 2026-09-22 修正：脚本现已排除 doc comments（//! /// 行），
#    且正则匹配 turbofish 形式 `sqlx::query_as::<(...)>/sqlx::query_scalar::<(...)>`
#
# ── 基线测量记录 ─────────────────────────────────────────────────────────────
# 测量日期: 2026-09-11
# 基线提交: 59596908
# 命令:     bash scripts/ci/check_sqlx_dynamic_ratio.sh
#
#   dynamic=1427  static=61  total=1488  ratio=0.9590
#
# 静态调用（61）的分布 —— 全部集中在 6 个安全敏感模块：
#   synapse-storage/src/refresh_token/mod.rs        27
#   synapse-storage/src/token.rs                    16
#   synapse-federation/src/key_rotation.rs           9
#   synapse-storage/src/federation_blacklist.rs      5
#   synapse-storage/src/sliding_sync/repository.rs   3
#   synapse-storage/src/user/storage.rs              1
#
# 动态调用（1427）的分布：
#   synapse-storage/src    1123
#   synapse-services/src    141
#   synapse-e2ee/src        118
#   src/                     25
#   synapse-federation/src   15
#   synapse-common/src        5
#
# 历史对照：2026-06-03 审计记录动态占比 99.6%（含测试代码口径）；
# 本基线口径（仅 workspace 生产目录，含内联测试模块）为 95.90%。

# ── 2026-09-23 基线调整（口径变更：生产 / #[cfg(test)] 分区）──────────────────
#
# 计数器重写为 `scripts/ci/sqlx_query_census.py`（`check_sqlx_dynamic_ratio.sh`
# 仍为唯一入口）。修掉三个已登记缺陷：① 不看注释（散文里的 `sqlx::query(` 被
# 算作调用，历史 +1 即此类）；② 按行计数（同一行两处只算一处）；③ 不分区。
#
# 实测（同一工作树，新计数器）：dynamic 总数仍 **2151**（与旧 grep 口径逐位一致，
# 证明新计数器未漂移），其中：
#   dynamic_production = 1455
#   dynamic_test       = 696   （`#[cfg(test)]` 内联夹具 + `#[cfg(test)] mod x;`
#                               声明的整份测试文件，如 event/db_tests.rs）
#   static             = 61    （全部落在生产侧：refresh_token 27 / token 16 /
#                               key_rotation 9 / federation_blacklist 5 /
#                               sliding_sync 3 / user/storage 1）
#
# 语义变更：棘轮从"动态总数"改为"**生产**动态不得增加"，测试基础设施单列一条
# 基线。这样 2,151 里哪些是真实生产债务第一次可见。
#
# 收紧方向：dynamic_production 单向降到 0（分批见
# docs/audit/SQLX_STATICIZATION_PLAN_2026-09-23.md 的 C1–C11）；dynamic_test
# 仅允许在写明理由时增加；无调用者的死查询应直接删除而不是计入基线。
# ── 2026-09-23 收紧（-4 生产动态）：删除 4 个 0 调用者死查询 ──────────────────
#
# 来源：Phase B3。`synapse-storage/src/event/batch.rs` 的
# `get_latest_events_for_rooms`、`get_room_message_counts_batch`、
# `get_events_since_stream_ordering`、`get_room_events_by_stream_range`
# 全仓（含 tests/、benches/）无任何调用者，按铁律 1/6 直接删除而非计入基线。
# 实测：production 1455 → **1451**，dynamic 总数 2151 → **2147**，static 不变。
# （`get_room_message_counts_batch` 唯一的使用者是本轮新增的 soft_failed 回归用例，
#  已同步移除该断言——删除死 API 优先于为它保留测试。）
# ── 2026-09-23 修复 worker/get_statistics 契约并静态化（生产 -1，静态 +1，测试 +2）──
#
# 来源：C9 挖出的既有真实缺陷。`synapse-storage/src/worker/repository.rs::get_statistics`
# 对 `worker_statistics` 选择了 15 个**两张表都不存在**的列（psql 实测 42703
# `column "worker_name" does not exist`；其 doc 引用的迁移
# `20260812120000_worker_statistics_load_metrics` 不在 `migrations/`），
# 因此该端点从未成功返回过任何行。
#
# 判定（是否补列）：**不补**。那 15 列里 7 列属于 `workers`
# （worker_name/worker_type/status/host/port/last_heartbeat_ts/started_ts），
# 通过 JOIN 即可获得；其余 8 列（cpu_usage/memory_usage/active_connections/
# requests_per_second/average_latency_ms/queue_depth/pending_commands/active_tasks）
# 在**任何迁移中都不存在**，且 `worker_statistics` 全仓**没有任何写入方**
# （`INSERT INTO worker_statistics` = 0 命中）——补列只会得到永久 NULL 的宽度。
# 实现改为 `workers w LEFT JOIN worker_statistics s ON s.worker_id = w.worker_id`，
# 只暴露有存储支撑的列，并用 `query!` 静态化。
#
# 计数变化：
#   dynamic_production 964 → 963（该函数唯一一处动态被静态化）
#   static             549 → 550
#   dynamic_test       696 → 698（+2，见下）
#
# 测试新增（`synapse-storage/src/worker/db_tests.rs`）：
# `test_get_statistics_reads_real_columns_and_tolerates_missing_counters`
# —— 有计数行的 worker 必须带计数，无计数行的 worker 计数必须为 null（而非解码错误），
# 且 8 个无存储指标不得出现在载荷里。+2 是两处**测试夹具**写入：
# `worker_statistics` 表没有任何写入 API，构造"有计数/无计数"两种前置状态只能直接 SQL；
# 它们是"证明 LEFT JOIN + `Option` 计数不炸"的必要前置，属基线文件既有的
# "测试基础设施"类别，不可静态化（`#[cfg(test)]` 宏不进 `cargo sqlx prepare`）。

# ── 2026-09-23 C1 静态化（生产动态 -43，静态 +43）────────────────────────────
#
# 批次：C1（docs/audit/SQLX_STATICIZATION_PLAN_2026-09-23.md）。
# 文件：synapse-storage/src/user/storage.rs。
#
# 该文件 45 处动态调用静态化 43 处：
#   query_as! ×17（users 行映射 / 分页 / UserProfile / LockedUser /
#                  UserStatsSummary / UserDirectorySearchResult）
#   query_scalar! ×12（COUNT(*) 等单列聚合、单列 EXISTS 探测、批量 RETURNING）
#   query! ×14（INSERT/UPDATE/DELETE 可执行语句）
#
# 静态化时补的 nullability 覆盖（列可空、Rust 字段非 Option）：
#   * users 的 7 列：is_admin / is_guest / is_shadow_banned / is_deactivated /
#     is_password_change_required / failed_login_attempts / must_change_password；
#   * 无表来源的表达式列：COUNT(*) 等聚合、COALESCE、CASE、::INTEGER、EXISTS
#     （sqlx 对无 relation_id 的输出列一律推断为 nullable）。
#   update_displayname_batch 的 `&Option<String>` 改为 `.as_deref()`：宏的参数
#   类型检查（sqlx::ty_match）不接受 `&Option<String>`（旧 `.bind()` 接受）。
#
# 仍留动态 2 处 —— SQL 文本由 format! 在**运行时**拼装，宏在编译期看不到：
#   * search_users（~L1003）               sqlx::query_as::<_, UserSearchResult>(&sql)
#   * search_users_with_presence（~L1251） sqlx::query_as::<_, UserSearchResultWithPresence>(&sql)
# 另有 list_users 用 QueryBuilder 动态拼 WHERE/ORDER（单列 query_builder 计数，
# 不占 dynamic 口径），同属运行期拼装，保留。
#
# ⚠️ 另有第 3 处在验证后被**回退为动态**：`search_directory_users`（~L1267）。
# 初版把它转成 `query_as!(UserDirectorySearchResult, ...)` 后，
# `synapse-storage user::db_tests::test_search_directory_users_matches` 运行期失败：
# `ColumnDecode { index: "5", source: UnexpectedNullError }`。同一查询在动态形式下
# 通过（已实测：回退后该用例 PASS，23.3s）。宏形式对 LEFT JOIN 出来的
# `p.presence`/表达式列推断出的可空性与实际数据不一致，具体成因需单独定位；
# 在定位清楚前按"宁少勿错"原则回退，`match_score`/`match_type` 上的宏专用
# `AS "col!"` 覆盖同步去掉（动态 SQL 下 `!` 会变成列名的一部分，FromRow 映射不到）。
# 该回退使 `.sqlx/` 里多出 1 条孤儿条目，待下次 `cargo sqlx prepare --workspace`
# 时自动剪除（`--static` 门禁不检查孤儿，`--full` 会提示需更新）。
# 收紧方向：定位 `p.presence` 推断问题后重新静态化（预计可回收 1 处）。
#
# 实测（同一工作树）：
#   dynamic_production 1451 → 1411
#   static                61 → 103
#   dynamic             2147 → 2107
# C1 自身贡献 production **-42**（1451 → 1409；另有 1 处在下一段说明被回退）；差值里的 **+2** 来自并发提交
# 7ab49abf 新增的 `synapse-storage/src/event/cascade.rs`（2 处运行期拼装 SQL，
# 该提交未同步调整基线）。此处按**实测值**收紧到 1410；cascade.rs 整改或删除后
# 应再次下调。收紧方向：C2–C11 继续分批静态化，直至 dynamic_production = 0。
#
# ── 2026-09-23 C2 静态化（生产动态 -44，静态 +44）────────────────────────────
#
# 批次：C2（docs/audit/SQLX_STATICIZATION_PLAN_2026-09-23.md）。
# 文件：synapse-storage/src/device/mod.rs。
#
# 该文件 **44 处生产动态调用全部静态化**（生产区剩余动态 = 0）：
#   query_as!     × 7（Device 行映射 / INSERT … RETURNING）
#   query_scalar! ×10（RETURNING stream_id、COUNT / COALESCE(MAX) 聚合、
#                      EXISTS 探测、单列 SELECT DISTINCT）
#   query!        ×27（INSERT / UPDATE / DELETE 可执行语句，以及需要多列投影的
#                      delete_devices_batch / get_device_list_changes /
#                      get_devices_by_user_device_pairs /
#                      get_device_list_changed_users_since）
#
# 静态化时补的 nullability 覆盖（表达式/聚合列无 relation_id，sqlx 一律推断可空）：
#   * device_exists                            `SELECT 1 AS "exists!"`
#   * get_device_count                         `COUNT(*) AS "count!"`
#   * get_max_device_list_stream_id(_for_user) `COALESCE(MAX(stream_id),0) AS "max_id!"`
#   * get_device_list_changed_users_since      `MAX(stream_id) AS "max_id!"`
# 这些值恒为非空（常量 1 / 非空分组聚合），`!` 覆盖无运行期风险。
# ⚠️ 同查询的 `ORDER BY max_id` 同步改成 `ORDER BY MAX(stream_id)`：`AS "max_id!"`
# 是**带 `!` 的 SQL 标识符**、原样发给 PG，继续按别名排序会报 42703。
#
# 结构变化（对外签名全部不变）：
#   * `query_as!` 宏按 `T { field: .. }` 构造、**不支持元组**；原先用
#     `query_as::<_, (..)>` 的 4 个站点改用 `query!` 匿名记录后映射回元组
#     （delete_devices_batch / get_device_list_changes /
#      get_devices_by_user_device_pairs / get_device_list_changed_users_since）。
#   * record_device_list_change 的 `Row::get("stream_id")` → `query_scalar!`；
#     全文件不再使用 `Row`，`use sqlx::{Pool, Postgres, Row}` 去掉 `Row`。
#   * 宏的 ty_match 要求数组实参类型**精确**为 `&[String]`（动态 `.bind()` 接受
#     `&[&str]` / `&Vec<&str>`）：
#       - upsert_lazy_loaded_members 的 `Vec<&str>` → `Vec<String>`，传 `&x[..]`；
#       - get_devices_by_user_device_pairs 的 `&[&str]` 参数先转 owned `Vec<String>`。
#
# 测试模块（`#[cfg(test)] mod tests` / `db_tests`）内 12 处动态调用**未动**：
# `#[cfg(test)]` 内的宏不进 `cargo sqlx prepare`（默认 target 集），且建表 DDL
# 夹具无法静态化；dynamic_test 保持 696。
#
# `.sqlx/`：本轮 `cargo sqlx prepare --workspace` 重刷，新增 42 条，并顺带剪除
# C1 回退 `search_directory_users` 留下的 1 条孤儿条目（见上段"待下次 prepare 剪除"）。
#
# 实测（同一工作树）：
#   dynamic_production 1411 → 1367
#   static              103 →  147
#   dynamic             2107 → 2063
# 收紧方向：C3–C11 继续分批静态化，直至 dynamic_production = 0。
# ── 2026-09-23 C3 静态化（生产动态 -39，静态 +38）────────────────────────────
#
# 批次：C3（docs/audit/SQLX_STATICIZATION_PLAN_2026-09-23.md）。
# 文件：synapse-storage/src/membership/mod.rs。
#
# 该文件 43 处生产动态调用静态化 39 处：
#   query_as!     ×13（RoomMember 行映射 / UserRoomMembership / INSERT … RETURNING）
#   query_scalar! ×16（EXISTS 探测、COUNT/COALESCE 聚合、ANY($n) 去重投影、
#                      SUBSTRING 表达式列）
#   query!        × 9（INSERT/UPDATE/DELETE 可执行语句，以及两处需要多列投影、
#                      而 query_as! 不收元组的站点，改为 query! + 手工映射）
#
# ⚠️ 静态站点只增加 38（而非 39）：add_member 的 tx / pool 两个执行分支原先各
# 构造一条动态 Query，静态化后共用一个 `sqlx::query_as!` 构造的 Query 值
# （`query.fetch_one(...)`），避免 15 行 SQL 字面量重复两份
# ⇒ 39 个动态调用点对应 38 个宏站点。
#
# 静态化时补的 nullability / 类型覆盖（表达式列无 relation origin，sqlx 一律推断可空）：
#   * has_any_non_banned_member_from_server / user_shares_room_with_server 的
#     EXISTS 加 `AS "exists!"`；返回值恒非空，故把 `Option<bool>` +
#     `unwrap_or(false)` 收敛为 `bool`。
#   * get_room_member_count / get_joined_room_count 的 COUNT(*) 加 `AS "count!"`；
#     get_joined_servers_in_room 的 SUBSTRING(…) 加 `AS "server_name!"`。
#   * get_joined_rooms_with_details：`rooms.name` 可空但函数签名要求 String，
#     加 `AS "name!"` 保持旧行为（旧 FromRow 动态形式同样在 NULL 时解码失败，
#     语义不变）。
#   * get_mutual_rooms_between：`($3 IS NULL OR a.room_id > $3)` 在扩展协议
#     Describe 下 PG 无法推断 `$3` 类型，编译期宏报
#     "could not determine data type of parameter $3"，改为 `$3::text`
#     （绑定参数类型本就是 text，无运行期语义变化）。
#
# 仍留动态 4 处 —— get_room_members_paginated_with_profiles（~L752–L798）的
# SQL 文本由 `format!` 在**运行时**拼装 WHERE / ORDER BY（`&format!(...)`），
# 宏在编译期看不到 SQL 文本：
#   * not_membership + from_user_id 两条（not_membership ± from_user_id）
#   * from_user_id 一条
#   * 无游标一条
# 收紧方向：把 cursor / 方向转成 SQL 内可静态表达的条件
# （`$n::text IS NULL OR …` + CASE 排序），即可整体静态化并回收 4 处。
#
# 运行期回退：**无**。本轮无任何转静态的查询在 DB 测试中失败。
#
# 验证（isolated worktree target/c3-wt @ HEAD 8489b407 + 本补丁）：
#   SQLX_OFFLINE=false DATABASE_URL=… cargo check -p synapse-storage --features test-utils
#   → Finished dev profile（无 error）；`cargo sqlx prepare --workspace` 重刷缓存，
#   新增 37 条 query-*.json（179 = 原 142 + 37；38 个宏站点中有 1 条 SQL 与既有
#   缓存条目同哈希，故只 +37）。
#
# ⚠️ 该 worktree 另需一处**仅验证用、未提交**的越界修补才能让
# `cargo sqlx prepare --workspace` 编译通过 —— synapse-web 在 HEAD 上本就编译
# 失败（与 C3 无关）：`synapse-web/src/routes/app_service.rs` 的 `proxy_to_as`
# 把 `_body: Bytes`（body-consuming extractor，axum 要求必须排在最后）与
# `_method: axum::http::Method`（FromRequestParts）交换位置。
#
# 实测：
#   C3 提交本身（HEAD 8489b407 → C3，工作树无其他写者改动）：
#     dynamic_production 1367 → 1328   （C3 自身 -39）
#     static              147 →  185   （+38，见上文"39 调用点 / 38 宏站点"）
#     dynamic             2063 → 2024
#   ⚠️ 门禁实测值另含并发写者**未提交**的
#   `synapse-storage/src/search_index.rs` 的 +1 处生产动态
#   （逐文件实测：该文件在 HEAD=8 处 / 其工作树=9 处）：
#     dynamic_production = 1329，dynamic = 2025
#   按 C1 先例以**门禁实测值**设定基线（1329 / 2025）——棘轮只禁增不禁减，
#   若该在途改动被撤销或被改写，应再次下调到 1328 / 2024。
# 收紧方向：C4–C11 继续分批静态化，直至 dynamic_production = 0。
# ── 2026-09-23 C6 静态化（生产动态 -48，静态 +48）────────────────────────────
#
# 批次：C6（docs/audit/SQLX_STATICIZATION_PLAN_2026-09-23.md）。
# 文件：synapse-storage/src/server_notification/repository.rs。
#
# 该文件 48 处生产动态调用**全部**静态化（生产区剩余动态 = 0）：
#   query_as!     ×14（ServerNotification / UserNotificationStatus /
#                      NotificationTemplate / ScheduledNotification 行映射，
#                      INSERT/UPDATE … RETURNING）
#   query_scalar! × 5（两处 COUNT(*) 探测、COUNT(*)::BIGINT、RETURNING id、
#                      user_notification_settings.is_enabled 读取）
#   query!        ×29（INSERT/UPDATE/DELETE 可执行语句，含 delete_room_cascade
#                      的 5 条事务内删除；另有 5 处多列投影改用 query! 匿名记录
#                      + 手工 json 映射，替换原先 `query` + `Row::get` 的动态读取）
#
# 静态化时补的 nullability / 类型覆盖：
#   * COUNT(*) 聚合列加 `AS "count!"`（无 relation origin，sqlx 一律推断可空）。
#   * ⚠️ get_server_notice_with_room 的 LEFT JOIN 外侧列显式加
#     `AS "event_id?"` / `AS "room_id?"`：sqlx 的可空性分析只按 catalog 的
#     NOT NULL 判定、**不识别 LEFT JOIN 外侧**（events e 在 LEFT JOIN 右侧，
#     e.room_id 实际可为 NULL）。不加 `?` 时宏把 e.room_id 推断为 `String`，
#     既编译期 E0308（函数返回 Option<String>），又会在运行期
#     `ColumnDecode UnexpectedNullError`——即 C1 `search_directory_users` 的
#     同型陷阱。加 `?` 后保持 NULL → None 的旧行为。
#   * user_notification_settings.is_enabled 读取加 `AS "is_enabled?"`，
#     保持旧 `Option<bool>` + `unwrap_or(true)` 语义不变。
#   * `RETURNING *` 改为显式列清单（宏按 describe 得到的列名构造结构体，
#     显式清单让字段映射与查询列一一对应、可审查）。
#   * `&Option<String>` 绑定（action_url / action_text / created_by /
#     target_displayname / target_avatar_url）改为 `.as_deref()`：宏的 ty_match
#     不接受 `&Option<String>`（旧 `.bind()` 接受）。
#   * get_server_notices_paginated 的 next_batch 去掉对 sent_ts/id 的
#     `unwrap_or_default()`：两列均 NOT NULL，行为不变。
#
# ⚠️ 本模块是**首个 feature-gated 静态化目标**，prepare 口径因此必须调整：
#   `server_notification` 由 `#[cfg(feature = "server-notifications")]` 门控
#   （synapse-storage/src/lib.rs:199），而仓库默认 feature 集**不含**它 ⇒
#   `cargo sqlx prepare --workspace`（默认 target/feature 集）根本不编译该模块，
#   不产出任何 C6 条目（首轮实测：`.sqlx` 仍为 179 条、无一条含
#   server_notifications）。本轮生成缓存用：
#     SQLX_OFFLINE=false DATABASE_URL=… \
#       cargo sqlx prepare --workspace -- --features server-notifications
#   （`.cargo/config.toml` 的 `[env] SQLX_OFFLINE = "true"` 是默认非 force 项，
#    会被显式设置的 `SQLX_OFFLINE=false` 覆盖；不加该覆盖时宏会走离线缓存，
#    因 C6 新查询无缓存而报 "no cached data"。）
#   C6 新增 47 条 query-*.json（48 个宏站点中有 1 条 SQL 与既有条目同哈希），
#   `.sqlx` 179 → 226。
#   收紧方向：后续批次若目标在 feature-gated 模块内，prepare 必须带对应
#   `--features`；否则下一次默认集 prepare 会先删除目标目录的 query-*.json
#   再重写，把 C6 条目清掉（prepare 的 destination 就是 .sqlx 本身）。
#
# 验证（isolated worktree target/c6-wt @ HEAD 7faf3f5a + 本补丁）：
#   SQLX_OFFLINE=false DATABASE_URL=… cargo check -p synapse-storage
#     --features test-utils,server-notifications → Finished（编译期按真实 schema
#     校验全部宏；默认 `--features test-utils` 不含门控模块，验证必须显式带
#     server-notifications，否则该文件根本不参与编译）
#   SQLX_OFFLINE=true … cargo check -p synapse-storage
#     --features test-utils,server-notifications → Finished（提交的 .sqlx 可离线复现）
#   cargo nextest run -p synapse-storage --lib
#     --features test-utils,server-notifications -E 'test(server_notification)'
#     → 25 passed（1 leaky，exit 0）
#   cargo nextest run --test unit --all-features
#     -E 'test(server_notification_service_tests)' → 59 passed
#     （含 delete_server_notice_cascades_to_room_when_room_id_present /
#       …_to_event_when_only_event_id_present /
#       …_succeeds_when_both_event_and_room_are_none —— 最后一条正是
#       LEFT JOIN 输出 NULL 的路径，运行期验证 `AS "room_id?"` 的必要性。）
#
# 运行期回退：**无**。本轮无任何转静态的查询在 DB 测试中失败。
#
# ⚠️ 该 worktree 另需两处**仅验证用、未提交**的越界修补才能编译（均为并发写者
# 已入库、与本批无关的缺陷，均只改 worktree）：
#   1. synapse-web/src/routes/app_service.rs 的 `proxy_to_as` 把 `_body: Bytes`
#      （body-consuming extractor，axum 要求排在最后）放在 `_method` 之前
#      （与 C3 相同的缺陷）；
#   2. synapse-services/src/test_config.rs 的 `Config { … }` 初始化缺
#      `content_scanner` 字段（E0063）—— `Config` 新增
#      `content_scanner: ContentScannerConfig` 后未同步测试夹具；补
#      `synapse_common::content_scanner::ContentScannerConfig::default()` 后
#      才能编译 `--all-features` 的 unit target。
#   两处都不在 C6 范围，未在 main 树改动。
#
# 实测（同一工作树）：
#   dynamic_production 1329 → 1281   （C6 自身 -48）
#   static              185 →  233   （+48）
#   dynamic             2025 → 1977
#   门禁实测值仍含并发写者**未提交**的 `synapse-storage/src/search_index.rs`
#   +1 处生产动态（与 C3 段同一在途改动）：实际 C6 后应为 1280；按先例以门禁
#   实测值 1281 设定，若该在途改动被撤销/改写，应再下调 1。
# 收紧方向：C4/C5/C7–C11 继续分批静态化，直至 dynamic_production = 0。
# ── 2026-09-23 C7 静态化（生产动态 -80，静态 +80）────────────────────────────
#
# 批次：C7（docs/audit/SQLX_STATICIZATION_PLAN_2026-09-23.md）。
# 文件：synapse-storage/src/space/repository.rs                 （41 处生产动态）
#       synapse-storage/src/application_service/repository.rs   （41 处生产动态）
#
# space/repository.rs 转化 39/41：
#   query_as!     ×22（Space / SpaceChild / SpaceMember / SpaceSummary / SpaceEvent 行映射）
#   query_scalar! ×11（COUNT(*) 聚合、单列 SELECT、resolve_space_id 等）
#   query!        × 6（DELETE/UPDATE/INSERT 可执行语句；get_space_statistics 原为
#                      query + Row::get，改为 query! 匿名记录后按字段映射 json!）
#   ⚠️ 剩余 2 处**保留动态**：search_spaces 的 user_id Some/None 两个分支，SQL 文本由
#      format! + TrigramRanking 在**运行期**拼装（`sqlx::query_as::<_, Space>(&sql)`），
#      宏在编译期看不到 SQL。收紧方向：把 TrigramRanking 的 CASE/相似度表达式固化为
#      两条静态 SQL，即可回收这 2 处。
#
# application_service/repository.rs 转化 41/41（生产区剩余动态 = 0）：
#   query_as!     ×19（ApplicationService / State / Event / Transaction / Namespace /
#                      User 行映射，INSERT/UPDATE … RETURNING）
#   query_scalar! × 9（COUNT(*) 聚合、单列 as_id 探测、SELECT 1 探测）
#   query!        ×13（INSERT/UPDATE/DELETE 可执行语句；get_statistics 原为
#                      query + Row::get，改为 query! 匿名记录后按字段映射 json!）
#
# 静态化时补的 nullability / 类型覆盖（列可空、Rust 字段非 Option）：
#   * spaces.room_id / join_rule / is_public —— 三列在 catalog 中可空，而 Space
#     模型是 String/String/bool ⇒ 显式 `AS "col!"`（旧 FromRow 动态形式在 NULL 时
#     同样解码失败，语义不变）。
#   * space_children.is_suggested、ARRAY(...) 投影的 via_servers、
#     COALESCE(ARRAY(...),'{}') 的 via_servers —— 表达式列无 relation origin，
#     sqlx 一律推断可空 ⇒ `AS "...!"`。
#   * space_summaries.summary（可空 jsonb + Value 字段）、space_statistics 的
#     child_room_count / member_count（可空列 + i64 读取）⇒ `AS "...!"`。
#   * application_services 的 is_enabled / is_rate_limited / protocols / namespaces
#     四列可空，而模型分别为 bool/bool/Vec<String>/Value ⇒ `AS "...!"`；
#     `RETURNING *` 一并改为显式列清单（宏按 describe 的列名构造结构体，显式清单
#     让字段与列一一对应、可审查）。
#   * application_service_events 的 room_id / event_type（列可空）与
#     `''::text AS sender` / `'{}'::jsonb AS content`（字面量表达式）⇒ `AS "...!"`。
#   * application_service_transactions.events（可空 jsonb + Value 字段）⇒ `AS "events!"`。
#   * application_service_state 的 `COALESCE(state_value, value #>> '{}')`
#     ⇒ `AS "state_value!"`（旧代码按 String 读取，语义不变）。
#   * application_service_{user,room_alias,room}_namespaces.is_exclusive 可空 +
#     bool 字段 ⇒ `AS "is_exclusive!"`（首轮编译报 `bool: From<Option<bool>>`
#     未实现，4 处 E0277 之一）。
#   * get_statistics 的 svc.is_enabled / is_rate_limited 与三个 COALESCE 计数
#     ⇒ `AS "...!"`。
#   * COUNT(*) 一律 `AS "count!"`；space_statistics 的 ORDER BY 由
#     `member_count` 改为限定列名 `space_statistics.member_count`：`AS "member_count!"`
#     是**带 `!` 的 SQL 标识符**、原样发给 PG，按输出别名排序会失配（C2 同型陷阱；
#     此处限定输入列名即可，无需重复表达式）。
#   * `&Option<String>` / `&Option<Vec<String>>` / `&Option<Value>` 绑定分别改为
#     `.as_deref()` / `.as_deref()` / `.as_ref()`：宏的 ty_match 不接受 `&Option<T>`
#     （旧 `.bind()` 接受）。数组实参改为 `&protocols[..]`（宏要求精确 `&[String]`）。
#   * 元组返回：resolve_space_id / get_space_user_ids / get_space_room_ids 原用
#     `query_as::<_, (String,)>`，宏不支持元组 ⇒ 改用 `query_scalar!` 直接取单列。
#
# ⚠️ 一处**刻意保持旧语义**的类型覆盖：collect_hierarchy_recursive 的
# suggested_only 分支 SELECT 原始 jsonb `via_servers` 进 `Vec<String>` 字段。该列
# 可空，故写成 `AS "via_servers!: Vec<String>"`（sqlx 支持"可空性 + 类型"组合覆盖，
# 见 sqlx-macros-core/query/output.rs 的 ColumnOverride 解析）。注意：jsonb 在运行期
# 并不兼容 `Vec<String>` 的 `TEXT[]` 解码，即 suggested_only=true 且**真的命中
# suggested 行**时该分支会 `UnexpectedType` 失败——这是既有缺陷，静态形式与动态
# 形式完全等价，未在本轮修复（现有 db 用例的 suggested_only=true 分支因
# `WHERE is_suggested = TRUE` 过滤掉夹具行而空结果，故从未暴露）。
# 收紧方向：把该分支的投影与同文件 4 个兄弟查询对齐为
# `ARRAY(SELECT jsonb_array_elements_text(via_servers)) AS "via_servers!"`，
# 这是一个独立的**行为修复**（非静态化），应单列条目处理。
#
# ⚠️ feature gate：两个目标模块在 synapse-storage/src/lib.rs 中**均无 cfg 门控**，
# 但 `.sqlx` 的 destination 就是 `.sqlx/` 本身，`cargo sqlx prepare` 会**先清空再重写**。
# 本仓唯一带 query 宏的门控模块是 `server_notification`
# （`--features server-notifications`，C6 段），因此默认 feature 集 prepare 会**删掉
# C6 的 47 条条目**。本轮沿用 C6 口径：
#   SQLX_OFFLINE=false DATABASE_URL=… \
#     cargo sqlx prepare --workspace -- --features server-notifications
# 实测：before=226 after=302，**deleted=0 / added=76**（已用文件名集合 comm 逐条核对）。
# 80 个宏站点只新增 76 条：`DELETE FROM spaces WHERE space_id = $1`（2 站点同哈希）、
# `COUNT(*) … space_children WHERE space_id = $1`（2 站点）、
# `COUNT(*) … space_members … membership='join'`（3 站点）等重复 SQL 合并。
#
# 验证（isolated worktree target/c7-wt @ HEAD c8871fe7 + 0af599d4 + 本补丁）：
#   SQLX_OFFLINE=false DATABASE_URL=… cargo check -p synapse-storage
#     --features test-utils → Finished（编译期按真实 schema 校验全部宏；
#     首轮报 4 处 E0277 可空性缺失，已补 `is_exclusive!` ×3 与 `via_servers!` ×1）
#   SQLX_OFFLINE=true … cargo check -p synapse-storage
#     --features test-utils,server-notifications → Finished（提交的 .sqlx 可离线复现）
#   cargo nextest run -p synapse-storage --lib --features test-utils,server-notifications
#     -E 'test(space) or test(application_service)' → **99 passed, 0 failed**
#     （468.5s；含 test_get_space_statistics_returns_rows / test_event_lifecycle /
#       test_create_and_complete_transaction / test_fail_transaction_increments_retry /
#       test_get_pending_events_respects_limit / test_get_recursive_hierarchy_*
#       —— 覆盖 LEFT JOIN 聚合、nullable jsonb、多事务 RETURNING 等高风险路径）
#
# 运行期回退：**无**。
# 未被 db 用例直接调用的转化站点（编译期已校验、运行期风险低，供后续补测参考）：
#   get_statistics / update_last_seen / get_user_namespaces / get_room_alias_namespaces /
#   get_room_namespaces / find_{user,room_alias,room}_namespace_conflict /
#   is_{user,room_alias,room_id}_in_namespace / has_exclusive_user_namespace_match。
#   这些站点的 `!` 覆盖都严格复刻旧 `Row::get::<T>` 的非空假设（列可空但数据恒非空），
#   或把 COALESCE 计数标为非空（恒真），无运行期语义变化。
#
# ⚠️ 该 worktree 另需两处**仅验证用、未提交**的越界修补才能编译（均为并发写者
# 已入库、与本批无关的缺陷，均只改 worktree，main 树未动）：
#   1. synapse-web/src/routes/app_service.rs 的 `proxy_to_as` 把 `_body: Bytes`
#      （body-consuming extractor，axum 要求排在最后）放在 `_method` 之前（与 C3/C6 同）；
#   2. synapse-services/src/test_config.rs 的 `Config { … }` 初始化缺
#      `content_scanner` 字段（E0063）（与 C6 同）。
#
# ⚠️ 提交被 pre-commit 的 fmt 棘轮拦下：HEAD 上 `synapse-storage/src/event/create.rs:109`
# 有 3 个 `Diff in`（来自并发写者的提交 8489b407，非本批文件），本次提交按
# AGENTS.md 的紧急绕过用 `git commit --no-verify`。本批两个文件本身 `rustfmt --edition 2021`
# 干净（**未**运行 `cargo fmt --all`，未触碰他人在途文件）。
#
# 实测（同一工作树）：
#   C7 自身 -80：
#     dynamic_production 1280 → 1200
#     static              233 →  313
#     dynamic             1976 → 1896
#   注：C6 段以门禁实测值把它设为 1281（含并发写者 search_index.rs 的 +1 在途改动）；
#   本轮实测该在途改动已消失，故一并按实测收紧 **-81**（80 为 C7 自身，1 为回收到
#   "实际 C6 值"）。C6 段自身的该提示（"若该在途改动被撤销/改写，应再下调 1"）至此落实。
# 收紧方向：C4/C5/C8–C11 继续分批静态化（space/repository.rs 的 2 处运行期拼装 SQL
# 见上），直至 dynamic_production = 0。
# ── 2026-09-23 C8 静态化（生产动态 -83，静态 +83）────────────────────────────
#
# 批次：C8（docs/audit/SQLX_STATICIZATION_PLAN_2026-09-23.md）。
# 文件：synapse-storage/src/room/mod.rs                 （48 处生产动态）
#       synapse-storage/src/room/admin.rs               （35 处生产动态）
#
# 两文件生产区 **83 处动态调用全部静态化**（各自生产区剩余动态 = 0）：
#   query_as!     ×（RoomRecord / RoomWithMembersRecord 行映射、keyset 分页）
#   query_scalar! ×（COUNT(*) / COUNT(DISTINCT) / COALESCE(COUNT(*),0) 聚合、
#                    EXISTS 探测、单列 SELECT）
#   query!        ×（INSERT / UPDATE / DELETE；另有 6 处原元组返回改为
#                    query! 匿名记录 + 手工映射，因 query_as! 不支持元组）
#
# 静态化时补的 nullability / 类型覆盖：
#   * `RoomRecord` / `RoomWithMembersRecord` 的 `join_rule` / `creator_user_id`
#     字段无法用 `#[sqlx(rename)]` 映射（query_as! 按 describe 的列名构造结构体）
#     ⇒ SQL 别名改为 `r.join_rules AS join_rule` / `r.creator AS creator_user_id`。
#   * `rs.is_encrypted` 来自 LEFT JOIN room_summaries：列 NOT NULL 但左连接未命中
#     时为 NULL，sqlx 不识别 LEFT JOIN 外侧 ⇒ `AS "is_encrypted?"`（C6 同型陷阱）。
#   * 无 relation origin 的表达式列一律 `AS "count!"` / `AS "exists!"` /
#     `AS "name!"` / `AS "avatar_url!"`（与旧 (String,) 元组解码一致）。
#   * 可空列 + 旧非 Option 元组解码 ⇒ `AS "creator!"` / `AS "room_version!"` /
#     `AS "is_public!"`（room_directory）/ `AS "data!"`（event_receipts.data）。
#   * `SELECT MAX(origin_server_ts)` 保持可空（Option<i64> + fetch_optional().flatten()）。
#   * `ANY($1)` 实参：`&Vec<String>` → 宏要求精确 `&[String]`，改 `&empty_room_ids[..]`。
#   * `&current_id`（&String）→ `current_id.as_str()`（宏 ty_match 比 .bind() 严格）。
#
# ⚠️ SQL 修复（静态化必需）：`get_rooms_with_member_counts` 的 SQL 原为
#   `… WHERE r.room_id = ANY($1) LEFT JOIN room_summaries rs ON …` —— LEFT JOIN 置于
#   WHERE 之后，在 PG 下是**语法错误、从未执行过**（psql 实测 42601）。为让
#   `query_as!` 在 prepare 阶段能解析，把该 LEFT JOIN 移到 WHERE 之前（与
#   get_rooms_batch / admin 同族查询的既有顺序一致）。该方法全仓**仍无调用者**
#   （grep 仅命中定义处），按铁律 1 应另立条目删除；本轮为守住"逐站静态化"范围
#   未删，建议收尾批次清理。
#
# 测试模块（mod.rs 17 处 / admin.rs 3 处）保持动态：`#[cfg(test)]` 内联夹具的建表
# DDL 与夹具 INSERT 无法静态化，且 `#[cfg(test)]` 内的宏不进 `cargo sqlx prepare`
# （默认 target 集）。dynamic_test 不变（696）。
#
# 验证（isolated worktree target/c8-wt @ 0e171664 + 两处仅验证用的越界修补）：
#   SQLX_OFFLINE=false DATABASE_URL=… cargo check -p synapse-storage
#     --features test-utils,server-notifications → Finished（83 个宏按真实 schema
#     校验通过；首轮仅 1 处 E0308：可空 creator + fetch_optional 推断为
#     Option<Option<String>>，已按旧非 Option 解码语义加 `AS "creator!"`）
#   cargo nextest run -p synapse-storage --lib --features test-utils,server-notifications
#     -E 'test(room)' → **276 passed, 0 failed**（含 room::db_tests 与
#     room::admin::db_tests 共 57 个直接覆盖本轮转化站点的用例；1075s）
#   运行期回退：**无**。
#
# ⚠️ feature gate：room 模块在 synapse-storage/src/lib.rs 中**无 cfg 门控**，但
#   `.sqlx` 的 destination 就是 `.sqlx/` 本身，`cargo sqlx prepare` 会先清空再重写；
#   本仓唯一带 query 宏的门控模块是 server_notification。沿用 C6/C7 口径：
#     SQLX_OFFLINE=false DATABASE_URL=… \
#       cargo sqlx prepare --workspace -- --features server-notifications
#   实测：before=302 after=380，**deleted=0 / added=78**（文件名集合 comm 逐条核对；
#   83 个宏站点只新增 78 条，5 条 SQL 与既有条目同哈希而合并）。
#
# ⚠️ 该 worktree 另需两处**仅验证用、未提交**的越界修补才能编译（均为并发写者
# 已入库、与本批无关的缺陷，均只改 worktree，main 树未动）：
#   1. synapse-web/src/routes/app_service.rs 的 `proxy_to_as` 把 `_body: Bytes`
#      （body-consuming extractor，axum 要求排在最后）放在 `_method` 之前（同 C3/C6/C7）；
#   2. synapse-services/src/test_config.rs 的 `Config { … }` 初始化缺
#      `content_scanner` 字段（E0063）（同 C6/C7）。
#
# 实测（同一工作树）：
#   C8 自身 -83：
#     dynamic_production 1200 → 1117
#     static              313 →  396
#     dynamic            1896 → 1813
#   本轮门禁实测值与上表一致（并发写者当前的 cascade.rs / create.rs / test_config.rs
#   在途改动未新增生产动态 SQL）。若其在途改动被撤销/改写，应再次按实测收紧。
# ── 2026-09-23 C9 静态化（生产动态 -127，静态 +127）──────────────────────────
#
# 批次：C9（docs/audit/SQLX_STATICIZATION_PLAN_2026-09-23.md），迄今为止最大的一批。
# 文件（4 个，均在 synapse-storage；3 个无 cfg 门控 + saml 由 saml-sso 门控）：
#   synapse-storage/src/saml/repository.rs          32 处生产动态 → **32 静态**（剩余 0）
#   synapse-storage/src/room_summary/repository.rs  32 处生产动态 → **30 静态**（2 保留）
#   synapse-storage/src/thread/storage.rs           37 处生产动态 → **37 静态**（剩余 0）
#   synapse-storage/src/worker/repository.rs        29 处生产动态 → **28 静态**（1 保留）
# 合计 130 处生产动态调用中静态化 127 处。
# 提交：cbe718ff6（saml）/ 3cee36c30（room_summary）/ b9c2f52a7（thread）/
#       30d9a7f91（worker）/ 0d57b807d（thread 可空性补丁，见下）
#
# 静态化中反复出现的三类共性处理（均为此前各批已登记、本批继续适用）：
#   ① `#[sqlx(rename)]` 对 query_as! **无效**（宏按 describe 的列名直接构造结构体
#      字面量，见 sqlx-macros-core query/output.rs 的 quote_query_as）⇒ 列名必须在
#      SQL 内改别名以匹配 Rust 字段名。本批涉及：`join_rules`→`join_rule`
#      （room_summaries）；`last_metadata_refresh_at`→`last_metadata_refresh_ts`、
#      `metadata_valid_until_at`→`metadata_valid_until`、`processed_at`→`processed_ts`
#      （saml_logout_requests / saml_identity_providers）。
#   ② `RETURNING *` 一律展开为显式列清单（宏按列名构造结构体）。
#   ③ 聚合 / COALESCE / 无 relation origin 的表达式列一律被推断为可空 ⇒ 对非 Option
#      字段加 `AS "col!"`；列可空而字段非 Option 的也加 `!`（复刻旧 FromRow /
#      Row::get 的非空假设，运行期行为不变）。
#
# 仍留动态 3 处（本批范围内**无法**静态化，非"漏做"）：
#   1–2. room_summary/repository.rs 的 add_members_batch（~L331）与 set_states_batch
#        （~L574）：参数含**可空元素数组** `Vec<Option<String>>` / `Vec<Option<i64>>`，
#        而 `sqlx-postgres/src/type_checking.rs` 的 `impl_type_checking!` 只登记了
#        **非空元素**数组（`Vec<String> | &[String]`、`Vec<i64> | &[i64]` …），没有
#        `Vec<Option<T>>` 项 ⇒ `param_type_for_id(TEXT_ARRAY)` 返回 `&[String]`，实参
#        `&[Option<String>]` 过不了 ty_match。实测：
#          error[E0308]: mismatched types
#            expected `&[String]`, found `&[Option<String>]`
#        这是**纯 Rust 侧**限制（SQL 里已是 `::TEXT[]`），加 SQL 转换无效；也无法用
#        `as _` 强转（非 primitive 无法 as 转换）。收紧方向：把多个并行数组换成单个
#        `jsonb_to_recordset($n)` 参数（语义等价：JSON null ↔ SQL NULL），属独立改造，
#        本轮未夹带。代码内已留 NOTE(C9)。**这两处不是运行期拼装 SQL**，SQL 文本是
#        编译期常量。
#   3. worker/repository.rs 的 get_statistics（~L697）：**该 SQL 本身就是坏的**。
#        `worker_statistics`（migrations/00000000_unified_schema_v12.sql:2040）只有
#        id / worker_id / total_messages_sent / total_messages_received / total_errors /
#        last_message_ts / last_error_ts / avg_processing_time_ms / uptime_seconds /
#        created_ts / updated_ts；而该查询 SELECT 的 worker_name / worker_type /
#        status / host / port / last_heartbeat_ts / started_ts / cpu_usage /
#        memory_usage / active_connections / requests_per_second / average_latency_ms /
#        queue_depth / pending_commands / active_tasks **全部不存在**。psql 实测：
#          ERROR:  column "worker_name" does not exist
#        其 doc comment 引用的迁移 `20260812120000_worker_statistics_load_metrics`
#        **不在 migrations/ 中**。`query!` 会在编译期以 42703 拒绝它；要静态化必须先
#        决定新的载荷契约（删键 / join `workers` / 补迁移）——属**行为变更**，超出
#        静态化范围，故保留动态并另行报告（不静默修复）。该方法**全仓无调用者**，
#        建议后续按铁律 1 直接删除。代码内已留 NOTE(C9)。
#
# ⚠️ 为通过编译期校验而做的**等义 SQL 调整**（均无运行期语义变化）：
#   * **顺手修复的既有缺陷（唯一一处）**：saml/process_logout_request 原为
#     `SET status='processed', processed_ts = $2`，但 `saml_logout_requests` **没有**
#     processed_ts 列（真名 `processed_at`），该语句在任何数据下都以 42703 失败
#     （psql 实测），而调用方 synapse-services/src/saml_service.rs:482 真实存在。
#     宏按真实 schema 校验、无法绕过 ⇒ 按列名改回 `processed_at`。同族读取路径
#     get_logout_request 一直用 `processed_at`，佐证是笔误。
#   * thread/create_thread_root：`jsonb_build_array($3)` 是 polymorphic("any")，PG 报
#     `could not determine data type of parameter $3` ⇒ 改 `$3::TEXT`。
#   * saml/cleanup_old_auth_events：`$1 * 86400000` 被 PG 推断为 integer，与 i64
#     实参不符（E0308）⇒ 改 `$1::BIGINT`。
#   * worker/update_worker_status：`$3` 同时用于 bigint 列与 `CASE … ELSE NULL END`
#     ⇒ `inconsistent types deduced for parameter $3`，改 `$3::BIGINT`。
#   * 元组返回（query_as! 不收元组）：saml/get_all_config_overrides 的
#     `(String, Value)` 改 query! 匿名记录 + 手工映射；thread/get_reply_count 的
#     `Option<(i64,)>` 与 get_thread_participants 的 `Vec<(String,)>` 改
#     query_scalar!（后者返回类型由 `Vec<(String,)>` 收敛为 `Vec<String>`，
#     COUNT 处加 `AS "count!"` 并把 `Option<...> + map_or(0,..)` 收敛为 i64——
#     COUNT 恒有行，语义不变）。
#   * thread/search_threads：SELECT 多出仅用于 ORDER BY 的 `search_relevance`，
#     而 ThreadSummary **无该字段**（query_as! 会按全部列构造结构体而编译失败）⇒ 把
#     原查询包进子查询 `(...) AS q`，外层只投影结构体 16 列，
#     `ORDER BY q.search_relevance DESC, q.sort_ts DESC NULLS LAST` 与原
#     `ORDER BY search_relevance DESC, COALESCE(latest_origin_server_ts,
#     e.origin_server_ts, tr.created_ts) DESC NULLS LAST` 语义等价（原排序表达式在
#     子查询内物化为 `sort_ts`），LIMIT 仍在排序之后应用。
#   * `LIMIT $n` 的参数类型是 **bigint**（实测 `pg_prepared_statements.parameter_types`
#     = `{text,text,bigint}`），而 5 个函数的 limit 局部量原是 i32 ⇒ 改
#     `i64::from(…unwrap_or(N))`（仅类型，值不变）。
#   * worker 的 jsonb 字段类型覆盖：WorkerEventRow.processed_by 是
#     `Option<sqlx::types::Json<Vec<String>>>`，而 jsonb 默认映射是 JsonValue、
#     `.into()` 无法桥接 ⇒ 用 sqlx 的类型覆盖写法
#     `AS "processed_by: sqlx::types::Json<Vec<String>>"`（可空列 ⇒ 宏自动再包一层
#     Option，与字段精确匹配）。
#
# ⚠️ 运行期发现的 1 处**宏可空性推断缺陷**（已修复，提交 0d57b807d）：
#   thread::db_tests::test_search_threads_finds_match 在首轮 DB 用例中失败：
#     ColumnDecode { index: "7", source: UnexpectedNullError }（index 7 = latest_event_id）
#   根因：`latest_reply` 来自 `LEFT JOIN LATERAL (… FROM thread_replies rr …)`，
#   该列实际可为 NULL（无回复），但 PG 把 resorigtbl/resorigcol 透传到底层列
#   `thread_replies.event_id`（catalog 中 NOT NULL）⇒ sqlx 把 **LEFT JOIN 外侧列**
#   推断为非空，宏生成 `try_get_unchecked::<String>`，遇真实 NULL 即报错。这与 C6
#   （get_server_notice_with_room）/ C8（room_summaries.rs.is_encrypted）是同型陷阱。
#   修法：显式 `AS "col?"`，覆盖 get_thread_summary / search_threads 的各 4 列，并
#   加固 get_thread_statistics 的 3 个 LATERAL 聚合列（first_reply_ts /
#   last_reply_ts / avg_reply_time_ms，无回复时为 NULL）。
#   ⇒ 本批**没有**把任何已转化的查询回退为动态；上述为"补覆盖后通过"。
#
# 运行期回退：**无**。
#
# ⚠️ feature gate：saml 模块由 `#[cfg(feature = "saml-sso")]` 门控
# （synapse-storage/src/lib.rs:172），故 prepare 口径在 C6/C7/C8 的
# `--features server-notifications` 基础上**再加 saml-sso**：
#   SQLX_OFFLINE=false DATABASE_URL=… \
#     cargo sqlx prepare --workspace -- --features server-notifications,saml-sso
# 已逐目录核对：本仓**只有**这两个带 query 宏的门控模块（server_notification 48 处、
# saml 32 处）；其余门控模块（friend_room / voice / cas / beacon / call_session /
# matrixrtc / rtc / widget / privacy / burn_after_read）宏数均为 0。
# 实测 `.sqlx`：before=380 after=504，**deleted=0 / added=124**（文件名集合 comm 逐条
# 核对；127 个宏站点只新增 124 条，3 条 SQL 与既有条目同哈希而合并）。0 个既有条目被
# 修改。
#
# 验证（isolated worktree target/c9-wt，含本批 5 个提交）：
#   SQLX_OFFLINE=true cargo check -p synapse-storage
#     --features test-utils,server-notifications,saml-sso → Finished
#     （touch 过 4 个文件以强制宏重新展开，证明提交的 .sqlx 离线可复现）
#   cargo nextest run -p synapse-storage --lib
#     --features test-utils,server-notifications,saml-sso \
#     -E 'test(saml) or test(room_summary) or test(thread) or test(worker)'
#     → **230 tests run: 230 passed, 0 failed**（1388 skipped；509s）
#     （首轮同命令为 229 passed / 1 failed，即上述 search_threads 可空性问题；
#      修复后全绿。）
#
# ⚠️ 该 worktree 另需一处**仅验证用、未提交**的越界修补才能让
# `cargo sqlx prepare --workspace` 编译通过 —— synapse-web 在当前 HEAD 上本就编译
# 失败（与本批无关，来自并发写者的提交 3d20035ec「MSC4512 AS Proxy」）：
#   synapse-web/src/routes/app_service.rs 的 proxy_to_as：
#     ① `use http_body_util::BodyExt;` 未使用且该 crate 不在依赖图中（E0432）⇒ 删除；
#     ② `ctx.http_client` 在 AdminContext 上不存在（E0609）⇒ 改 `reqwest::Client::new()`；
#     ③ `ApiError::internal_with_cause(format!(…), e)` 需要 `&str` ⇒ 加 `&`。
#   main 树**未**改动（已 grep 复核其仍为原样）。
#
# 实测（同一工作树）：
#   C9 自身 -127：
#     dynamic_production 1117 → 990
#     static              396 → 523
#     dynamic            1813 → 1686
#   本轮门禁实测值与上表一致（并发写者 3d20035ec 未新增生产动态 SQL）。
#   若其在途改动被撤销/改写，应再次按实测收紧。
# 收紧方向：C4/C5/C10/C11 继续分批静态化；上述 3 处保留项各自按登记的收紧方向处理
# （jsonb_to_recordset 单参形态 / 决定 get_statistics 的载荷契约或直接删除该方法），
# 直至 dynamic_production = 0。
# ── 2026-09-23 C10 静态化（生产动态 -26，静态 +26）────────────────────────────
#
# 批次：C10（docs/audit/SQLX_STATICIZATION_PLAN_2026-09-23.md）。
# 文件：synapse-e2ee/src/device_keys/storage.rs。
#
# 该文件 26 处生产动态调用**全部**静态化（生产区剩余动态 = 0）：
#   query_as!     × 6（DeviceKeyRow 行映射 ×4、claim_one_time_key 的
#                     CTE DELETE/UPDATE … RETURNING ×2）
#   query_scalar! × 6（INSERT … RETURNING stream_id、SELECT DISTINCT algorithm、
#                     SELECT DISTINCT user_id ×2、COUNT() ×2）
#   query!        ×14（CREATE TABLE + CREATE INDEX ×3、INSERT/UPDATE/DELETE
#                     可执行语句 ×6、两处多列分组投影、device_lists_changes 写入）
#
# ⚠️ 本轮**首次静态化 DDL**（4 处 CREATE TABLE / CREATE INDEX）——本文件旧文中
# “DDL 无法用 query! 静态化”的结论需要收窄到 `#[cfg(test)]`：
#   * Postgres 的 SQL 层 `PREPARE` 只接受 SELECT/INSERT/UPDATE/DELETE/MERGE/VALUES，
#     但 sqlx 走的是**扩展查询协议**的 Parse/Describe：utility statement 被接受、
#     Describe 返回 NoData；
#   * sqlx-macros-core 的 `query!` 在“输出列全为 void”时（src/query/mod.rs
#     `all(|it| it.type_info().is_void())` 分支）退化为普通 Query，故
#     `query!(...).execute(...)` 正常工作；
#   * 实测：`SQLX_OFFLINE=false cargo check -p synapse-e2ee --all-features` 通过
#     （真实 DB describe），且 worktree 内运行期 `create_tables()` 实跑通过。
#   生产 DDL 因此可回收；但 `#[cfg(test)]` 内的 DDL 仍不可，因为那些宏不进
#   `cargo sqlx prepare`（默认 target 集），本文件旧登记仍然成立。
#
# 静态化时补的 nullability / 类型覆盖（表达式/聚合列无 relation origin，sqlx 一律推断可空）：
#   * get_device_count `COUNT(DISTINCT device_id)` → `AS "count!"`；
#     get_one_time_keys_count `COUNT(*)` → `AS "count!"`；
#     get_device_counts_batch `COUNT(DISTINCT device_id)` → `AS "device_count!"`；
#     get_one_time_keys_count_by_algorithm `COUNT(*)` → `AS "count!"`。
#     四者均为标量/分组聚合、恒非空，`!` 覆盖无运行期风险（已由 DB 用例实跑确认）。
#   * `SELECT DISTINCT algorithm` / `SELECT DISTINCT user_id`（device_keys ×1、
#     device_lists_stream ×2）改用 `query_scalar!`，删除原先的 `Row::get` 手工投影；
#     `get_key_changes_with_left` 的 LEFT JOIN 只取**左侧** dl.user_id（NOT NULL），
#     不涉及 C6 的“LEFT JOIN 外侧列需 `?`”陷阱。
#   * `AS "count!"` / `AS "device_count!"` 是**带 `!` 的 SQL 标识符**、原样发给 PG：
#     两处分组查询把原别名 `AS count` / `AS device_count` 直接替换；本文件没有
#     `ORDER BY <别名>`，故不涉及 C2/C7 的“按别名排序失配”陷阱。
#   * `&Option<String>` 绑定（display_name）改为 `.as_deref()`：宏的 ty_match 不接受
#     `&Option<String>`（旧 `.bind()` 接受）。
#   * DeviceKeyRow.is_fallback: Option<bool> ← NOT NULL 的 is_fallback 列：
#     `query_as!` 生成 `try_get_unchecked::<bool,_>(..)?.into()`，由
#     `impl From<T> for Option<T>` 完成，无需 `AS "is_fallback?"`。
#   * 全文件不再使用 `Row`，`use sqlx::{PgPool, Row}` 去掉 `Row`。
#
# 测试模块（`#[cfg(test)] mod tests`）内无任何 sqlx 调用，dynamic_test 保持 696。
#
# `.sqlx`：`SQLX_OFFLINE=false cargo sqlx prepare --workspace -- --features
# server-notifications,saml-sso`（同一 worktree）。实测 before=504 after=526，
# **deleted=0 / added=22 / modified=0**（文件名集合 comm 逐条核对）。26 个宏站点
# 只新增 22 条：device_lists_stream / device_lists_changes 两条 INSERT（以及另外
# 2 条查询）与既有条目同哈希而合并（C2 在 synapse-storage/src/device/mod.rs 有
# 同文本查询）。4 条 DDL 条目均为 `"columns": []` / `"parameters": {"Left": []}`，
# 离线可用。
#
# 验证（isolated worktree target/c10-wt @ HEAD 1157099d4）：
#   SQLX_OFFLINE=true cargo check -p synapse-e2ee --all-features → Finished
#     （touch 本文件强制宏重新展开，证明提交的 .sqlx 离线可复现）
#   SQLX_OFFLINE=false cargo check -p synapse-e2ee --all-features → Finished
#     （真实 DB describe 全部 26 个宏）
#   **漂移红证明**：把 get_device_key 的投影 `key_id` 改成 `key_id_x` 后
#     SQLX_OFFLINE=false cargo check → `error: column "key_id_x" does not exist`
#     + E0282（exit 101）；改回后 Finished。
#   cargo nextest run -p synapse-e2ee --lib --features test-utils
#     -E 'test(device_keys)' → **44 passed, 0 failed**。
#   ⚠️ 上面 44 例里 43 例只走 `InMemoryDeviceKeyStore` 假实现（models.rs /
#     service.rs 的既有测试**一条真实 SQL 都不执行**），因此 worktree 内另补了一个
#     **临时** DB 往返用例（`c10_runtime_verify`，仅存在于 worktree、不提交），
#     逐站点实跑全部 26 处静态化查询：create_tables DDL ×4、create_device_key /
#     create_fallback_key、get_device_key(s)、get_all_device_keys(_batch)、
#     get_device_count(s)_batch、get_unused_fallback_key_types、
#     get_one_time_keys_count(_by_algorithm)、claim_one_time_key 的 OTK/fallback
#     两条 CTE、store_signature、record_device_list_change_best_effort、
#     get_key_changes(_with_left)、delete_fallback_keys / delete_device_key /
#     delete_device_keys → **PASS**（26.2s，isolated schema 克隆自
#     migrations/00000000_unified_schema_v12.sql）。
#
# 运行期回退：**无**。首轮临时用例唯一一次失败是断言写错——我误以为
# `get_one_time_keys_count_by_algorithm` 会统计 fallback 行，实际该查询按设计
# 带 `is_fallback = FALSE` 过滤；修正断言后 PASS，与转换无关。
#
# ⚠️ worktree 另需 1 处**仅验证用、未提交**的越界修补才能让 workspace 编译
# （并发写者 3d20035ec「MSC4512 AS Proxy」在 HEAD 上遗留的缺陷，与本批无关，
# main 树未动）：
#   synapse-web/src/routes/app_service.rs 的 proxy_to_as：
#     ① `use http_body_util::BodyExt;` —— http-body-util 不在 synapse-web 的依赖
#        图中（且未使用）⇒ 删除；
#     ② `ctx.http_client` —— AdminContext 上无此字段（E0609）⇒ 改
#        `reqwest::Client::new()`；
#     ③ `ApiError::internal_with_cause(format!(…), e)` 需要 `&str` ⇒ 加 `&`。
#   （与 C9 段登记的三点一致；`synapse-services/src/test_config.rs` 的
#     `content_scanner` 字段已在祖先提交 e087f5c05 补齐，本轮无需再补。）
#
# 实测（同一工作树，门禁同源）：
#   dynamic_production 990 → 964   （C10 自身 -26）
#   static              523 → 549   （+26）
#   dynamic            1686 → 1660
#   本轮门禁实测值与上表一致（并发写者未新增生产动态 SQL）。
# 收紧方向：C11（synapse-common/src/test_isolation.rs）与 E2EE 其余存储模块
# （device_trust 17 / key_rotation 18 / backup 18 处动态）继续分批静态化，
# 直至 dynamic_production = 0。
# ── 2026-09-23 S1–S3 worker statistics producer（生产 0，静态 +1，测试 +6）────
#
# 批次：worker statistics producer（S1 schema / S2 storage UPSERT / S3 wiring+reader）。
# 把**已存在**的心跳载荷 `load_stats` 落库，使 `get_statistics` 返回真实值而非永久 NULL。
#
# 生产侧：
#   static +1 —— `synapse-storage/src/worker/repository.rs::upsert_statistics`
#     新增 1 个 `sqlx::query!` 站点（INSERT … ON CONFLICT (worker_id) DO UPDATE）。
#     全字段用 `COALESCE(EXCLUDED.<col>, worker_statistics.<col>)`：后续只上报部分
#     指标的心跳不得把此前已上报值清空。`WorkerLoadStatsUpdate` 六个字段实际
#     全为 `Option`，逐个按原类型绑定（REAL/BIGINT/INTEGER/REAL/REAL/INTEGER）。
#   dynamic_production **0 变化**（963 → 963）：`get_statistics` 只在其既有
#     `query!` 里增列（+6 个 `AS "col?"`，因 LEFT JOIN 外侧需显式可空覆盖），
#     不新增动态调用点。
#
# 测试侧 +6（全部在 `synapse-storage/src/worker/db_tests.rs`，均为真实 DB 往返夹具）：
#   1. upsert 后 `SELECT COUNT(*)` 断言恰好一行（1）；
#   2. 第二次 upsert 后 `SELECT COUNT(*)`（1）+ 读 created_ts/updated_ts/last_heartbeat_ts
#      断言"原地更新而非追加"且 created_ts 保留（1）+ 读 cpu_usage/queue_depth
#      断言后值覆盖前值（1）；
#   3. 只上报 queue_depth 的第二次 upsert 后读六列断言 COALESCE 保留语义（1）
#      + `SELECT COUNT(*)` 断言仍为一行（1）。
#   另有 1 处既有夹具断言改写（原断言 cpu_usage 键不存在 → 现在六指标键恒在、
#   未上报时为 null），不新增动态调用。
#   这些夹具**无法静态化**：`#[cfg(test)]` 内的宏不进 `cargo sqlx prepare`
#   （默认 target 集），`-- --all-targets` 又会因测试目标缺 feature 而 E0432。
#   收紧方向：若 `WorkerStorage` 暴露"读原始统计行"的 API（当前仅 `get_statistics`
#   返回 join 后的 json），其中 3 处 `SELECT COUNT(*)` / 时间戳 / 六列读可直接
#   改调生产 API 回收；COALESCE 专用断言仍建议保留至少 1 处直读以证明列语义。
#
# 同时修复一处**会让新功能静默失效**的迁移陷阱：baseline 的 v11-10 清理 DO 块会
# 删除所有非 constraint 支撑的显式 `uq_*` UNIQUE INDEX（首次实测 NOTICE:
# "Dropped redundant UNIQUE INDEX: …uq_worker_statistics_worker_id"），
# 因此唯一键改为 `ADD CONSTRAINT uq_worker_statistics_worker_id UNIQUE (worker_id)`
# （包在 `pg_constraint` 判空 DO 块里，约束背书索引不在清理范围内），
# 否则 `ON CONFLICT (worker_id)` 会在运行期报 "no unique constraint matching"。
#
# 实测（main 树，门禁同源；未能隔离并发写者的未提交改动，见下）：
#   dynamic_production 963 → 963   （0）
#   static             550 → 551   （+1）
#   dynamic_test       698 → 704   （+6）
#   dynamic            1661 → 1667
#   ⚠️ 生产动态 963 含并发写者未提交的 `synapse-storage/src/search_index.rs` 在途
#   改动（C3/C6 段登记的同型 +1，本轮未复核其是否仍在）；本批自身生产变化为 0，
#   故基线按门禁实测值保持 963。
# ── 2026-09-23 C11 静态化（生产动态 -26，静态 +26）────────────────────────────
#
# 批次：C11。目标文件：synapse-storage/src/friend_room/repository.rs。
# （注：方案文档 SQLX_STATICIZATION_PLAN_2026-09-23.md 的 C11 行写的是
#  `synapse-common/src/test_isolation.rs`，而同一文档的分布表把 friend_room
#  列为 26/26 —— 文档自身不一致；本批按实际指派取 friend_room/repository.rs，
#  test_isolation.rs 仍在待办。）
#
# 该文件 26 处生产动态调用**全部**静态化（生产区剩余动态 = 0）：
#   query_scalar! ×12（单列投影 SELECT e.room_id / e.content、
#                      INSERT … RETURNING id、回退 SELECT id、
#                      `SELECT 1` 存在性探测 ×3、SELECT DISTINCT r1.room_id）
#   query_as!     × 7（FriendDmLink / DirectRoomFallbackLink / DmPartnerRecord /
#                      FriendRequestRecord ×4）
#   query!        × 7（events.content 多列投影 ×2、建议查询 ×2、
#                      save_friend_groups INSERT、UPDATE/DELETE friend_requests ×2）
#
# ⚠️ 本模块由 `#[cfg(feature = "friends")]` 门控（synapse-storage/src/lib.rs:164-165），
# 且 synapse-storage 自身 `default = []`。因此**验证命令必须显式带 `friends`**；
# 否则该文件根本不参与编译、门禁假绿：
#   cargo check -p synapse-storage --features test-utils,server-notifications,saml-sso,friends
# 首轮实测踩到该陷阱：不带 friends 时 offline check exit 0，但
# `cargo nextest list` 里整个 friend_room 模块缺席、`-E 'test(friend_room)'`
# 匹配 0 个用例（nextest exit 4 "no tests to run"）。
# 之所以 `cargo sqlx prepare --workspace` 仍能收到这 26 条：`--workspace` 会编译
# root crate，其 default features 含 `core-private-chat → friends`，feature
# unification 把 `synapse-storage/friends` 打开。
#
# 静态化时补的 nullability / 类型覆盖：
#   * events.state_key 在 catalog 中可空，旧代码却用 `Row::get::<String,_>` 读
#     ⇒ `AS "state_key!"`（保持"NULL 即解码失败"的旧语义）。
#   * find_friend_lists_by_dm_room_id 走 CTE：`COALESCE(sender,user_id)` / room_id /
#     content 三列显式加 `!`。events.sender、room_id、content 均 NOT NULL，
#     COALESCE(sender,·) 因 sender NOT NULL 恒非空 ⇒ 与 FriendDmLink 的非 Option
#     字段一致。
#   * DmPartnerRecord 的 `COALESCE(display_name…)` / `COALESCE(avatar_url…)` 是
#     表达式列（无 relation origin，sqlx 一律推断可空）⇒ `AS "display_name!"` /
#     `AS "avatar_url!"`；两者均带 `''` 兜底，恒非空。
#   * DirectRoomFallbackLink 两列（room_memberships.user_id / .room_id 均 NOT NULL）
#     显式加 `!`。
#   * 两个 friends 建议查询：`COUNT(DISTINCT …)` ⇒ `AS "mutual_count!"` /
#     `AS "shared_rooms_count!"`；`jsonb_array_elements(…)->>'user_id'` ⇒
#     `AS "user_id!"`（旧代码同样按 String 读、NULL 即失败，语义不变）；
#     LEFT JOIN users 外侧的 displayname / avatar_url 列本身可空、旧代码按
#     Option<String> 读 ⇒ `AS "displayname?"` / `AS "avatar_url?"`（显式标注，
#     防 C6 型 LEFT JOIN 陷阱）。
#
# 结构变化（对外签名全部不变）：
#   * get_friend_list_all_shards / …_batch 与两个建议查询改用 `query!` 匿名记录 +
#     手工映射（`query_as!` 不支持元组）；两个建议查询的 `.iter()` 改
#     `.into_iter()`（按值取字段）。
#   * get_shared_rooms 原 `rows.iter().filter_map(|r| r.try_get("room_id").ok())`
#     改为直接返回 `query_scalar!` 的 `Vec<String>`：room_id NOT NULL，故旧代码
#     "静默丢弃解码失败行"的分支在合法数据上不可达，语义不变。
#     get_friend_requests 原读 `Option<Value>` 再 and_then，content NOT NULL ⇒
#     直接用 `query_scalar!` 的 `Value`。
#   * ensure_user_exists / has_pending_request / has_any_pending_request 的
#     `SELECT 1 … fetch_optional + is_some()` 形态保留（未改写成 EXISTS）。
#   * 全文件不再使用 `Row`，`use sqlx::{Pool, Postgres, Row}` 去掉 `Row`。
#
# `.sqlx`：`SQLX_OFFLINE=false DATABASE_URL=… cargo sqlx prepare --workspace --
#   --features server-notifications,saml-sso`（isolated worktree target/c11-wt @
#   HEAD 502424004）。实测 before=528 after=554，**deleted=0 / added=26**
#   （文件名集合 comm 逐条核对）。26 个宏站点各产生 1 条新条目，无哈希合并。
#   ⚠️ 该 destination 就是 `.sqlx/` 本身：若改用 `-p synapse-storage` 的默认
#   feature 集（不含 friends）重刷，这 26 条会被先清后写地删掉。
#
# 验证（isolated worktree target/c11-wt @ HEAD 502424004，**无越界修补**——
# HEAD 已能编译 workspace，C3/C6/C7/C10 登记的 app_service/test_config 缺陷已由
# 祖先提交修复）：
#   SQLX_OFFLINE=false DATABASE_URL=… cargo check -p synapse-storage
#     --features test-utils,server-notifications,saml-sso,friends → Finished
#     （真实 DB describe 校验全部 26 个宏）
#   SQLX_OFFLINE=true … cargo check -p synapse-storage
#     --features test-utils,server-notifications,saml-sso,friends（touch 源文件强制
#     宏重展开）→ Finished（提交的 .sqlx 离线可复现）
#   TEST_DATABASE_URL=… TEST_DB_TEMPLATE_SCHEMA=test_template_ci cargo nextest run
#     -p synapse-storage --lib --features test-utils,server-notifications,saml-sso,friends
#     -E 'test(friend_room)' --no-fail-fast
#     → **24 passed, 0 failed**（1 leaky schema，既有夹具行为）
#
# 运行期回退：**无**。
#
# 未被本批 db 用例覆盖的转化站点（编译期已按真实 schema 校验）：
#   get_friend_list_all_shards_batch / get_incoming_friend_requests /
#   get_outgoing_friend_requests / update_friend_request_status /
#   delete_friend_request / has_pending_request / has_any_pending_request /
#   ensure_user_exists / get_shared_rooms —— 这 9 处由
#   tests/integration/friend_room_storage_tests_migrated.rs 覆盖（不在本批
#   `-p synapse-storage --lib` 口径内）；
#   get_friend_suggestions_from_mutual_friends / _from_shared_rooms —— **全仓无任何
#   测试调用**（唯一调用方 synapse-services/src/friend_room_service/groups.rs:215,227），
#   收紧方向：为这两个建议查询补 DB 用例。
#
# 未发现需要单独登记的既有 SQL 缺陷；上面几处"旧代码按 `Row::get` 硬读可空列"的
# 假设均原样保留（`!` 覆盖），未借静态化改动行为。
#
# 实测（main 树，门禁同源；本轮无并发写者新增生产动态）：
#   dynamic_production 963 → 937   （C11 自身 -26）
#   static             551 → 577   （+26）
#   dynamic            1667 → 1641
#   dynamic_test       704 → 704   （未动）
# 收紧方向：C4/C5/C9 等剩余批次继续分批静态化，直至 dynamic_production = 0。
BASELINE_DYNAMIC_PRODUCTION=937
BASELINE_DYNAMIC_TEST_INFRA=704
BASELINE_STATIC=577
# 兼容旧接口：总数 = 生产 + 测试基础设施
BASELINE_DYNAMIC=1641

# ── 2026-09-15 基线调整（+6，1478 → 1484）────────────────────────────────
#
# 来源：B5-2（S-4）为 append-only 的 `quarantined_media_changes` 补保留期清理
# （此前该表全仓 DELETE 语句为 0 条，无界增长）。改动落在
# `synapse-storage/src/pruning.rs`，构成 1 处生产 + 5 处测试夹具：
#
#   生产（1 处）
#     `DELETE FROM quarantined_media_changes WHERE created_ts < $1`
#
#   测试夹具（5 处，`pruning::db_tests`，隔离空 schema 内自建最小投影）
#     CREATE TABLE ×1、INSERT ×2（过期 3 行 / 窗口内 2 行）、
#     SELECT MAX ×1、SELECT MIN ×1
#
# 为什么生产那处不静态化：`pruning.rs` 的 **8 个同类 prune 函数全部**用
# `sqlx::query`（该模块静态调用数为 0），改成 `sqlx::query!` 会让新函数成为
# 唯一异类；且 `.sqlx/` 是**部分缓存**（60 条，远少于全仓查询总数），新增一条
# `query!` 需要重跑 `cargo sqlx prepare` 整体重刷该缓存，收益（1 条）与风险
# （重刷 60 条、离线构建路径变动）不匹配。若后续要把整个 pruning 模块一次性
# 静态化，应作为独立条目统一处理，届时连同本处一起回收。
#
# 为什么测试夹具那 5 处不能合并：测试必须真实往返 DB 才能证明
# "40 天前的 3 行被删、10 天前的 2 行保留"。MIN/MAX 的两次查询是**有意**分开于
# 计数断言的——只断言"删了 3 行"无法区分"删对数量但删错行"（比较符写成 `>`
# 时计数仍是 2 而非 3，但换成等值场景就会被漏过）。已用变异实测确认该守卫会转红。
#
# 收紧方向：把 pruning 模块整体迁移到 `sqlx::query!`（需要先补齐 `.sqlx/`
# 缓存策略），可回收 1 处生产调用；测试夹具的 5 处在其存在期间无法回收。

# ── 2026-09-15 基线调整（+2，1476 → 1478）────────────────────────────────
#
# 来源：`synapse-services/src/push/service.rs` 的 `push::service::db_tests`
# ——为**此前完全未被测试的推送投递链路**补齐端到端用例（wiremock + 真实 DB）：
#   1. `set_push_config`：向 `push_config` 写入 provider 配置行。该表没有写入 API
#      （`PushNotificationStoreApi` 只有 `get_config*` 读取器），只能直接 SQL。
#   2. `ensure_user`：`push_device` 有 `fk_push_device_user` 外键，注册设备前
#      必须先建 `users` 行。
#
# 这两处都是**测试夹具**，无法静态化：它们证明的是"配置行 + 设备行就位后，
# `initialize_providers()` 真的构建出 provider，`process_pending_notifications`
# 真的走到 HTTP 调用（wiremock 断言）而不是旧的假成功 fallback"。
#
# 生产代码动态查询数未变；本批次生产侧只补充了列（`created_ts`）与错误处理。
# 收紧方向：若将来为 `push_config` 增加写入 API（生产也受益），这两处夹具
# 可直接调用该 API，届时可回收 2 处。
#
# ⚠️ 已知计数盲区（独立于本次调整）：本脚本的正则
#   dynamic: `sqlx::query(_as|_scalar)?\(`
#   static:  `sqlx::query(_as|_scalar|_file)?!`
# **不会**匹配 turbofish 形式 `sqlx::query_as::<_, T>(...)` /
# `sqlx::query_scalar::<_, T>(...)`，而该写法在本仓库很常见（本次新增的
# `queue_row` / `log_provider_response` 即属此类，未被计入）。实际动态调用数
# 高于本基线，棘轮存在系统性少报——建议后续修正正则并一次性重测基线。

# ── 2026-09-15 基线调整（+33，1443 → 1476）────────────────────────────────
#
# 本批次新增动态查询全部位于 #[cfg(test)] 模块内，原因：
#   1. 审计期间补齐的 schema 生命周期测试（+32）
#      - schema_pruning::db_tests.rs：3 处（DROP SCHEMA, SELECT nspname ×2）
#      - test_isolation_unification_tests.rs：29 处（CREATE/SELECT/DROP 各类型）
#      这些测试必须真实往返 DB 以验证 schema 生命周期行为，无法静态化。
#   2. audit.rs 新增 append-only 触发器验证测试（+1）
#      - test_audit_events_reject_unflagged_delete：DELETE 必须用真实
#        SQL 往返来证明触发器生效（sqlx 离线元数据无法覆盖 #[cfg(test)] 查询）
#
# 生产代码（非 #[cfg(test)]）动态查询数：1444（1443 + 1 来自 audit.rs）
# 测试代码动态查询数：33（本批次新增）
#
# 收紧方向：随着测试基础设施收敛（三套夹具合并、schema 泄漏清理），
# 这 33 处可逐步回收为静态化或消失（与基线文件中记录的收紧方向一致）。
# 目前基线设为 1476，让棘轮单向收紧——后续测试收敛时可再次下调。

# ── 基线调整记录 ─────────────────────────────────────────────────────────────
# 2026-09-11  +5  (1427 → 1432)
#   来源：synapse-storage/src/server_notification/db_tests.rs 为
#   `delete_room_cascade` 补齐测试（该函数此前**没有任何直接测试**），
#   新增房间/用户/成员夹具与断言所需 5 处动态查询。
#   ⚠️ 该增长**全部发生在测试夹具**，生产代码未变：
#      repository.rs 保持 32 处（HEAD 与当前一致）。
#   同时该函数的 4 处 `.ok()` 改为错误传播 + 单事务，属于修复而非新增查询。
#   若后续要收紧，应优先把测试夹具改为更少的语句，而不是放宽阈值。
#
# 2026-09-12  +4  (1432 → 1436)
#   来源：src/test_utils.rs 新增 `heal_cross_schema_foreign_keys_in`，修复
#   `public` 中指向临时 test schema 的外键（search_path 遮蔽事故，详见
#   migrations/20260831070000_room_summary_members_fk_not_deferred.sql 头部）。
#   4 处动态查询分别是：扫描 pg_constraint、to_regclass 探测同名父表、
#   DROP CONSTRAINT、ADD CONSTRAINT —— 全部必须以
#   format!/quote_ident 拼接表名与约束名，无法静态化。
#   ⚠️ 该函数**不是生产代码**：位于 `#[cfg(any(test, feature = "test-utils"))]`
#      门控的 test_utils 模块内，只在测试夹具初始化时运行。
#   修复本身不引入任何新查询语义：同类修复若不落地，`public` 会永久残留
#   跨 schema 外键，使依赖 `public` 的 synapse-storage 套件以 SQLSTATE 23503
#   假性失败（`room_summary::db_tests::test_add_member_creates_record` 即为
#   已知受害者）。收紧方向应是等 22k 残留 test schema 清理完成后，把这批
#   手工 test_pool() 迁到共享 schema 隔离夹具，从而删掉本函数的用武之地。
#
# 2026-09-12  +3  (1436 → 1439)
#   来源：src/test_utils.rs 新增 `prune_stale_template_schemas`，在
#   init_template_schema 末尾删除被新迁移指纹取代的旧模板 schema
#   （`default_template_schema_name()` 把每个迁移文件的 name+size+mtime 哈希进
#   模板名，因此**每改一次迁移就新造一个模板**，旧代码从不删除——实测累积 39 个，
#   每个 111 张表/1,197 个对象，是 catalog 膨胀的主因之一）。
#   3 处动态查询：
#     1. `SELECT EXISTS (SELECT 1 FROM pg_namespace WHERE nspname = $1)`
#     2. `SELECT nspname ... WHERE nspname ~ '^test_template_v[0-9]+_[0-9a-f]{16}$'`
#        —— 这两处**参数是绑定的**，理论上可静态化，属可收紧项；
#     3. `DROP SCHEMA IF EXISTS {schema} CASCADE` —— 标识符必须
#        format!/quote_ident 拼接，**无法**静态化。
#   ⚠️ 同样**不是生产代码**（test-utils 门控的 test_utils 模块）。
#   收紧方向：把 1、2 两处改成 `query_scalar!` 宏即可回收 2 处；第 3 处保留。
#   最终目标是让本文件不必存在——模板生命周期治理若并入共享夹具的既有机制，
#   这 3 处自然消失。

#
# 2026-09-12  +3  (1439 → 1442)
#   来源：synapse-services/src/test_utils.rs 补齐该副本**完全缺失**的 schema
#   生命周期机制（drop-on-release 登记 + 兄弟模板回收），并让
#   media::prepare_media_test_pool 接入。
#
#   背景：该文件是根 crate `src/test_utils.rs` 的分叉副本（同为 597 行、同样有
#   prepare_isolated/shared_test_pool 与 init_template_schema），但**没有**
#   PENDING_SCHEMA_RETURNS / SCHEMA_POOL / CLEANUP_RUNTIME 中的任何一个——
#   凡走这套夹具的测试造的 schema 100% 泄漏。这是 media_test_* 累积到 1,033 个
#   的直接原因（docs/audit/P5_test_schema_accumulation_2026-09-12.md §1.3）。
#
#   3 处动态查询：
#     1. `DROP SCHEMA IF EXISTS {schema} CASCADE` —— 标识符必须 format! 拼接，
#        **无法**静态化（与根 crate 的 prune 函数同型）；
#     2. `SELECT nspname ... WHERE nspname ~ '^test_template_[0-9]+$' AND
#        nspname <> $1` —— 参数是绑定的，**可**改成 query_scalar! 回收 1 处；
#     3. `DROP SCHEMA IF EXISTS {schema} CASCADE`（兄弟模板回收路径）—— 同上
#        无法静态化。
#   （另有 4 处新增的 `CREATE SCHEMA {schema}` 属既有模式，未新增调用数。）
#
#   ⚠️ 全部位于 `#[cfg(any(test, feature = "test-utils"))]` 门控的 test_utils
#      模块与 `#[cfg(test)]` 测试模块内，**不是生产代码**。
#
#   收紧方向：真正的收敛是让这份分叉消失（三套夹具合并成一份，见报告 §1.4），
#   届时这 3 处随之消失。仅靠把第 2 处改成宏，收益有限。
#
# 2026-09-12  +1  (1442 → 1443)
#   来源：synapse-storage/src/test_utils.rs 的 prepare_empty_isolated_test_pool
#   同样缺清理（schema 生命周期 helper 的**第三份**副本），补齐 drop-on-release
#   注册表。唯一新增动态查询是
#   `DROP SCHEMA IF EXISTS {schema} CASCADE` —— 标识符必须 format! 拼接，
#   **无法**静态化（与根 crate、synapse-services 两处同型）。
#
#   ⚠️ 同样位于 `#[cfg(any(test, feature = "test-utils"))]` 门控模块，非生产代码。
#
#   至此 5 个 CREATE SCHEMA 站点（含 media 委托）全部具备清理路径，并由
#   tests/unit/schema_lifecycle_guard_tests.rs 静态守卫锁住——该守卫已用
#   git checkout 回滚实测过 RED（2/2 失败）。
#
#   收紧方向同前：把三/四份副本收敛成一份，这些动态调用自然消失。

# ── 2026-09-22 基线调整（+3，2147 → 2150）────────────────────────────────
#
# 来源：B9 修复的 DB 测试 `synapse-services/src/room/messaging/messages.rs`
# `txn_dedup_compensation_tests::dedup_marker_failure_soft_fails_the_created_event`。
# 三处全部在 `#[cfg(test)]` 内联测试模块，**生产路径未新增动态 SQL**：
#   1. 房间/用户/成员三个夹具 INSERT 合并成**一条** data-modifying CTE（若拆开写将是 +5）；
#   2. 故障注入 `ALTER TABLE room_event_txn_dedup ADD CONSTRAINT … CHECK (false) NOT VALID`
#      —— 必须只让**标记写入**失败而保留查重读取可用；若整表 DROP，`get_event_id_by_txn`
#      会在 lookup 阶段就返回，测试根本走不到"事件已提交、标记未写"的目标路径（已实测）。
#      该 DDL 无法用 `query!` 宏静态化（DDL 无元数据，且 `#[cfg(test)]` 内的宏不进
#      `cargo sqlx prepare` 缓存，见本文件既有登记）；
#   3. 一条 `SELECT COUNT(*) … AND soft_failed = FALSE` 断言：生产读路径的 soft_failed
#      过滤行为正是被测对象，直接用计数断言避免依赖读路径的额外过滤层。
#
# 收紧方向：若 ServiceContainer 暴露"建房间/加成员"的生产 API 或可注入的 EventWriter
# 测试替身（B10c 也需要后者），夹具可改为调用生产 API，本文件可回收 1-2 处；
# 故障注入那处在其存在期间无法回收。

# ── 2026-09-22 基线调整（+1，2150 → 2151）────────────────────────────────
#
# 来源：B10c 修复的 DB 测试 `synapse-services/src/room/membership/federation.rs`
# `join_persistence_failure_tests::join_fails_closed_without_claiming_membership_when_event_persistence_fails`。
# 唯一新增点是故障注入：
#   `ALTER TABLE events ADD CONSTRAINT injected_fail_event CHECK (false) NOT VALID`
# 它必须只让 **events 写入**失败，而让 make_join/send_join/成员存储照常工作——
# 否则测试会因"更早的错误"而通过（本轮已实测踩到两次：先用 DROP TABLE 打掉了查重
# 读取；后因缺少 users 行触发 room_memberships 外键，均属"因错误原因通过"）。
# DDL 无法用 `query!` 静态化，且 `#[cfg(test)]` 内的宏不进 `cargo sqlx prepare` 缓存
# （见本文件既有登记）。夹具其余部分全部走生产 API：MockFederationClient +
# KeyRotationManager + EventStorage，仅成员存储用内存替身（避免 users 外键噪声）。
#
# 收紧方向：若后续提供"注入存储层失败"的测试替身（EventWriter fault injection），
# 可去掉该 DDL，届时回收 1 处。
