{
  "prod_unsafe_total": 0,
  "test_unsafe_total": 9,
  "note": "cargo-geiger one-way ratchet for BOTH counters: production may only go DOWN (and an unexplained decrease fails, so the number cannot rot into a loose ceiling), test-only may not go UP. 2026-09-22 tightening: the 2 production units recorded on 2026-09-21 were relocated into test targets by the B' landing (commit 298f74b8) and the baseline was never updated, so the gate had been reporting `production unsafe decreased (2 -> 0)`. Re-measured with the gate itself (`python3 scripts/ci/run_cargo_geiger.py`, artifact `artifacts/cargo-geiger.json`): all 10 workspace packages report 0 in every counter of the prod scan, `packages_without_metrics` is empty (nothing silently skipped), and the with-tests scan reports 9 test-only units. Test-only therefore went 8 -> 9, and that +1 is itemised below as the hand-written `libc::atexit` that B' moved out of the production build. Background: docs/audit/archive/GATE_INTEGRITY_FOLLOWUP_2026-09-19_LOG.md.",
  "prod_unsafe_sites": [],
  "test_unsafe_sites": [
    {
      "package": "synapse-common",
      "count": 2,
      "why": "Hand-written `#[cfg(test)]` blocks calling `std::env::set_var`/`remove_var` in synapse-common/src/config/mod.rs (:759, :924) — Rust 2024 requires `unsafe` for those.",
      "review_by": "2026-12-21"
    },
    {
      "package": "synapse-services",
      "count": 2,
      "why": "Same pattern in synapse-services/src/worker/topology_validator.rs (:663, :675, inside `mod tests`).",
      "review_by": "2026-12-21"
    },
    {
      "package": "synapse-rust",
      "count": 5,
      "why": "Two kinds, both test-build-only (the production build is 0): (a) 1 hand-written unit — `src/test_exit_hook.rs:29`, `unsafe { libc::atexit(synapse_common::test_schema_guard::drain_schemas_at_exit) }`, added by the B' landing (298f74b8) so the process-exit schema drain is registered without any `unsafe` in a shipped binary; `libc` is a `[dev-dependencies]` entry. (b) 4 cargo-geiger span/target attribution products (one per test target) from macro expansion — the same `format_args!` / `.await`-tokio-macro class as the historical entries; `tests/` itself contains no `unsafe` literal. This 5 is the old 4 plus (a); the production side of B' is the 2 -> 0 decrease that this file now records.",
      "review_by": "2026-12-21"
    }
  ]
}
