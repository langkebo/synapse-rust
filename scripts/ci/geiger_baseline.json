{
  "test_unsafe_total": 0,
  "note": "Ratchet for TEST unsafe only (files under tests/). Production unsafe is a hard ZERO with no allowlist and no baseline ceiling: scripts/ci/run_cargo_geiger.py rejects any baseline key it does not read, so a `prod_unsafe_total` entry here would contradict the enforced policy without changing it (that is exactly what happened between 2026-07-23 and 2026-09-18). The previous version of this file recorded `prod_unsafe_total: 4`, which were the #[cfg(test)] blocks `std::env::set_var`/`remove_var` in synapse-common/src/config/mod.rs (inside mod tests at :310; unsafe at :759 and :924) and synapse-services/src/worker/topology_validator.rs (mod tests at :385; unsafe at :665 and :677). They are excluded by running cargo-geiger WITHOUT `--include-tests` (asserted by validate_geiger_cmd), because cargo-geiger reports per FILE and the gate classifies by file path, so it cannot separate a #[cfg(test)] block from shipping code in the same src/ file. Update this file only to RAISE the test-unsafe baseline, with a justification."
}
